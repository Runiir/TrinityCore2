# Cataclysm raid progression handoff

## 2026-08-13T08:42:27Z — Exact aa85 build and fresh Phase 1 preparation passed

Mandatory independent Sol-high review of exact clean checkpoint `aa85a9d0b9cc34f9a73ce9b5ad9380af5879d3c4` reports zero Critical/High findings and authorizes the build only; no live or fidelity result is inferred. The exact policy-v8 configure receipt verifies gate-bearing. The first single-compiler worldserver attempt was terminated as `build_resource_abort` after a transient memory-PSI-full threshold while more than 17 GiB remained available; it is retained as infrastructure evidence and is not a code/gameplay failure. The immediate exact-source retry reused the completed objects, linked successfully, and independently verifies as gate-bearing. The receipt-produced ELF SHA-256 is `a58655770ce841e39829c4291e3f0421845c82e97be6e3d6381df0e57b13a767`. The build worktree remains clean at exact `aa85a9d0b9` and the focused runtime/capture/Phase 9 suite passes 116/116.

With no server, capture, publisher, or build active, deterministic BWD preparation reapplied 50 account and 5,768 character statements plus the exact route-start cleanup/mutation. The independent full verifier passes all 50 expected characters and all 31 runtime rotation profiles. Corpse-phase-aware v4 readback proves the exact frozen 2/3/5 roster offline and unleased at map 669 `(-345.872,-224.344,193.127)`, with full stat seeds and zero instance, group, ghost-aura, corpse, or corpse-phase residue. The frozen route is exactly 11 ordered nodes: only the entrance junction, Magmaw Chainwielder, and exact Drudge pair precede Magmaw at step 4; Omnotron begins at step 5.

The nine-file preparation bundle is pushed and remote-verified at `artifacts/cata_raid_program/phase1_preparation_aa85a9d0b9_20260813.dvc`, object `7a81cf466d0bba3f2665b7177e071cad.dir`, 2,031,042 bytes. Compact hashes are retained in `cata_raid_phase1_preparation_aa85a9d0b9_summary_v1.json`. Exact next gate: retain the current preparation payload only until capture preflight consumes it; start and verify exactly one receipt-bound worldserver, attach one Luna-xhigh babysitter, measure CPU/log/DB behavior, and run the uncapped canonical Phase 1 capture. Require Laser Strike exits, independent exact Drudge Rush/lane/reseparation/taunt/health-sync/trained-profile evidence, Magmaw engagement, full per-GUID native recovery, controlled-unit readiness, post-recovery ready check, cleanup, DVC publication, and exact eviction.

## 2026-08-13T08:16:41Z — Exact Spell-owned Rush delivery pairing remediated

Mandatory Sol-high review of exact clean checkpoint `10c5a969e48e145f53b939e80acf5fc57357be6b` confirmed the delivery, attempt/wipe/route, overflow, independent-capture, roster-reseparation, tank-taunt/health-sync, and trained-profile gates, but found one remaining High: a later successful Rush from the same source to the same target could mark the oldest canceled preparation as landed. It also noted two Medium robustness gaps: runtime source GUID was not independently validated and a script-prevented default effect could still reach the prior callback. No build or service was started.

Exact clean code checkpoint `a3c42d0e6420a32e92025e6a8bc4f48b21756f72` binds preparation to delivery with a per-Spell observation sequence. `NotifyNativeCreatureSpellStarted` returns the exact sequence, the Spell instance retains it, and only that exact sequence/source/target/attempt/wipe/route can land. Delivery is emitted inside `HandleEffects` only after the non-prevented default `SPELL_EFFECT_CHARGE` hit handler executes, so canceled, missed, vanished, script-prevented, or older same-target preparations cannot be completed retroactively. Independent capture reconstruction now also requires positive stable runtime GUIDs for both exact Drudge spawns and requires them to be distinct.

The focused runtime/capture/Phase 9 suite remains 116/116, Python compilation and diff checking pass, and the worktree is clean. Exact next gate is another independent Sol-high zero-Critical/High review of the clean status descendant; only then may the single policy-v8 coordinator build run. Live restart remains held.

## 2026-08-13T08:04:46Z — Native Drudge delivery and independent tactic evidence remediated

Mandatory Sol-high review of exact clean status checkpoint `70c053e22735e8f6951d8883753d516b0aefdbd4` closed forced recovery assistance, controlled-unit readiness, exact Magmaw engagement/wipe binding, tank health synchronization, and canonical status/DVC lineage. It rejected the build gate on three remaining Drudge evidence issues: a prepared Rush could be consumed before delivery, observations lacked attempt/wipe identity, and the canonical capture could pass without reconstructing Rush/reseparation/tactic decisions. No build or service was started.

Exact clean code checkpoint `6c759502542b634f63668abe115631551ef5368d` closes those gaps. Spell preparation freezes native source/target/range/interval selection, but delivery becomes true only after the real `SPELL_EFFECT_CHARGE` hit handler runs. Every observation carries attempt, wipe, and route generations; current strategy consumes only delivered matching-scope observations, same-node wipes clear stale queue state, and bounded queue eviction is a visible acceptance failure. The first Rush no longer self-certifies a 20-second interval.

RaidRuntime retains the exact evidence after leaving the Drudge node: prepared/delivered counts, per-source delivery and valid-interval counts, each delivered observation's ten-player reseparation acknowledgements, both tank taunts, tank health-sync participants, and trained single-target profile participants. Canonical acceptance independently reconstructs rather than trusts those counters. It requires two delivered Rushes from each exact spawn, at least one non-early 20-second interval per source, exact ten-player reseparation, both exact tank taunts, a tank participating in health-sync hold, and all seven tank/DPS slots executing trained single-target profiles. Prepared-only, stale-scope, overflowed, wrong-source/target/range/interval, incomplete-reseparation, missing-taunt/sync/profile evidence fails closed.

The focused runtime/capture/Phase 9 suite passes 116 tests, Python compilation and diff checking pass, and the worktree is clean. The previously pushed `validation_scenarios` object remains remotely retained with its workspace and exact five child cache objects locally evicted. Exact next gate: mandatory independent Sol-high zero-Critical/High review of the clean status descendant covering `70c053e227..HEAD`; only then may one policy-v8 coordinator build run. Live restart remains held pending that receipt, CPU measurement, fresh provisioning/full verifier/v4 readback, and a prestarted verified worldserver.

## 2026-08-13T07:45:27Z — Six mandatory-review High findings remediated at exact clean checkpoint

Observer authserver/worldserver processes, capture writers, publishers, and builds remain stopped. Their stale PID files and approximately 30 MiB of observer logs were removed; MySQL remains healthy. No live restart is authorized.

Exact code checkpoint `2b63c544855a1d30abd6ef60c1362667bff8b2f6` closes the six High findings from the mandatory Sol-high review of `d5ba8a07`. The recovery hold now uses only the existing all-offense authority and performs no forced combat stop, interrupt, attack stop, idle movement, teleport, or resurrection. Post-wipe readiness requires native hunter-pet restoration plus idle/alive controlled units. Capture acceptance is bound to exact Magmaw route generation/node and native boss engagement before the wipe. Native Drudge Rush is captured at successful spell preparation in an ordered bounded queue with exact source, target, pre-charge distance, declared range, and per-source interval evidence. Both assigned tanks now obey health synchronization while retaining independent lane taunt and separation duties.

The focused runtime/capture/Phase 9 suite passes 114 tests, Python compilation and diff checking pass, and no server or heavyweight build ran. `validation_scenarios` is pushed and remotely synchronized at object `0829bfe3c4c81f59b24fd4cbc0542974.dir`, 144,215 bytes and five files. After consuming it for this diagnosis, the workspace and exactly its five child cache objects were removed while the directory metadata was retained; no broad DVC garbage collection was used.

Exact next gate: obtain mandatory independent Sol-high zero-Critical/High approval of the clean status descendant covering the full `d5ba8a07..HEAD` delta, then run exactly one serialized policy-v8 coordinator build. Only after a valid exact receipt may root measure single-cohort CPU/log/DB behavior, reprovision, retain the full verifier and v4 readback, prestart and verify one worldserver, and attach one Luna-xhigh Phase 1 babysitter. The uncapped Magmaw-only capture must prove Laser Strike exits, exact Drudge lane/Rush/health-sync choices, trained damage-profile use, Magmaw engagement, full identity-bound native recovery, controlled readiness, post-recovery ready check, cleanup, DVC publication, and exact eviction. Six parallel boss shards remain downstream of a passing Phase 1 gate.

## 2026-08-13T07:12:31Z — Combined Phase 1 remediation ready for exact checkpoint and review

All observer services and experiment/build/publisher processes are stopped. The exact BWD fixtures were deterministically reprovisioned after the observer session; v4 readback passed with zero instance/group/ghost/corpse/corpse-phase residue. The historical `1ee02778ef` binary remains stale and no live restart is authorized.

The dirty integration checkpoint combines identity-bound native recovery, a pre-decision idempotent all-offense hold, radial-and-frontal hazard correction, stable blocked/unstuck resolution, exact Laser Strike union-safe movement, and bounded repeated worldport logs. Expensive progression/policy scoring now runs only on decision ticks or an actual stuck edge. The scheduler remains locked to one cohort until Phase 1 passes and per-cohort scheduling/isolation exists.

The BWD route is exactly eleven nodes. Phase 1 reaches only the entrance junction, Magmaw Chainwielder, the exact Drudge pair, and Magmaw. Omnotron Golem Sentries follow Magmaw. The Drudge node binds spawns 250140/250141 to two permanent five-player lanes with tanks 1/2, derives native-path anchors from their home spawns, keeps non-tanks outside both 15-yard Thunderclap circles, observes native Rush, reassigns tank ownership after charges, forbids area/multidot, balances lane health without an invented threshold, and waits for native Vengeful Rage after the first death. The unverified Rush-impact radius was removed.

`validation_scenarios` was regenerated through Pixi/DVC, pushed, and local/cloud status are synchronized at object `0829bfe3c4c81f59b24fd4cbc0542974.dir`. The focused runtime/capture suite passes 111 tests and `git diff --check` is clean. Next gate: commit with the required trailer, obtain exact Sol-high zero-Critical/High review, then run exactly one coordinated build. After a valid receipt, measure CPU/log/DB behavior, reprovision/read back, and execute the uncapped canonical capture. Publish, verify, diagnose, and exactly evict evidence before considering six parallel boss shards.

## 2026-08-13T05:22:12Z — Exact 1ee Phase 1 reached Magmaw; native recovery evidence remediation next

The receipt-bound `1ee02778ef85aff82887e1917d2c5ac7c3a33112` worldserver completed an uncapped canonical BWD 10N capture after fresh provisioning and v4 readback. The exact 2/3/5 roster formed in map 669 instance/save 2, killed all five declared entry/corridor targets, advanced to native Magmaw, and produced three full wipes. Future-encounter offensive authority remained closed and no forbidden assistance was observed.

The capture correctly failed as `infrastructure_abort`. Per-GUID death sequences advanced through 30, and trace rows contained native release/runback decisions followed by bots re-entering and re-engaging. However, identity-bound RaidRuntime evidence never recorded corpse, release, runback, re-entry, resurrection, or the post-recovery native ready check. Recovery generation stayed zero, and the uncapped monitor stopped the loop after 304.525 seconds of monotonic semantic stall. Cleanup independently proved zero bots, leases, and worldserver/capture processes.

The eight-file evidence bundle is pushed and remote-verified at `artifacts/cata_raid_program/phase1_raid_foundation_1ee02778_20260813.dvc`, object `6ac9cd047fdd69c6ee8a86100cc17d2a.dir`, 248,379,796 bytes. It contains raw JSONL, report, worldserver log, both build receipts, the full verifier, v4 readback, and exact route manifest. Compact hashes and classification are retained in `cata_raid_phase1_infrastructure_capture_1ee02778ef_summary_v1.json`. The workspace and exactly fourteen unique capture/preparation child cache objects were removed while both directory metadata objects and pointers remain; no broad DVC garbage collection was used.

The scoped Luna-xhigh remediation includes only exact corpse-authorized native release/runback worldports in admitted refreshes, uses the frozen native corpse-authority predicate instead of map-local corpse lookup, and reconstructs the guarded release-to-outside transition as an ordered runback edge. Focused runtime/capture tests pass 102. Mandatory independent Sol-high review is required before an exact commit/build. A separate authserver plus observer-only `1ee` worldserver is currently running for passive human inspection; it was freshly reprovisioned, has 10/10 bots in BWD 10N instance 3, is explicitly non-certifying, and must be stopped before any canonical rebuild/preflight/rerun.

Exact next gate: independent Sol-high review of the recovery observer and combat-hold behavior, then commit/rebuild the approved descendant, fresh provisioning/full verifier/v4 readback, and another uncapped canonical run. Require exact per-GUID death→corpse→release→runback→re-entry→resurrection order and completed native ready check before combat resumes. Parallel boss shards remain downstream of a passing Phase 1 gate.

## 2026-08-13T04:56:45Z — Fresh Phase 1 preparation passed, published, and evicted

With no worldserver, capture writer, publisher, or build active, deterministic BWD preparation for the receipt-bound `1ee02778ef85aff82887e1917d2c5ac7c3a33112` binary reapplied 50 account statements, 5,768 character statements, and four exact route-start statements. The full live verifier passed all 50 expected accounts/characters, generated payload, gear, spells, talents, glyphs, profiles, and enabled actions. Independent v4 readback proved the exact permanent BWD ten in the expected 2/3/5 identity at map 669 `(-345.872,-224.344,193.127)`, all offline and unleased, with full health/power seeds and zero character-instance, group-member, ghost-aura, corpse, or corpse-phase residue.

The nine-file preparation proof is pushed and remote-verified at `artifacts/cata_raid_program/phase1_preparation_1ee02778ef_20260813.dvc`, object `7a610416141f5b72d8861c98e769cbda.dir`, 2,001,105 bytes. Compact hashes and gate facts are retained in `cata_raid_phase1_preparation_1ee02778ef_summary_v1.json`. The workspace and exactly nine child cache objects were removed while the pointer and `.dir` metadata remain; no broad DVC garbage collection was used.

Live start is now authorized. Root must start and verify exactly one worldserver from ELF SHA-256 `128edf0720d514945727269618ef68aea09bbc064eba8be8bd15db5bd63b86f4` before attaching one Luna-xhigh Phase 1 babysitter. The uncapped capture remains the gate and must compare native Magmaw script lifecycle with every bot decision; no fidelity inference is allowed from the smoke alone.

## 2026-08-13T04:51:31Z — Exact gate-bearing build succeeded; fresh preparation next

Source-equivalent checkpoint `1ee02778ef85aff82887e1917d2c5ac7c3a33112` now has a successful policy-v8 configure and serialized one-compiler worldserver build from the clean isolated Phase 1 worktree. Both trusted-local receipts independently verify as gate-bearing: configure receipt SHA-256 `64dbdc99a9555dd81db973ad0f4efda1876fb4378aae8beed83227f97751b3a9`, worldserver receipt SHA-256 `3af19ab9a853fe026f13909d1273630d5643a38ad93c3d68987be19c4810e4bb`, and produced ELF SHA-256 `128edf0720d514945727269618ef68aea09bbc064eba8be8bd15db5bd63b86f4`. Request, admission, and completion source identities are identical and clean; the exact CMake graph, toolchain, policy, and runiir UID 1000 operator identity match. MySQL stayed healthy, memory PSI remained zero, no compiler/worldserver orphan remained, and the focused foundation/runtime/capture/research/pet-totem/Phase 9 partition passed 256 tests.

Live restart remains held. Exact next action is fresh deterministic reprovisioning, the independent full verifier, and corpse-phase-aware v4 database readback with compact retained proof. Only after those gates pass may root start and verify the single worldserver and attach one Luna-xhigh Phase 1 babysitter. The uncapped capture must independently prove corridor isolation, native Magmaw engagement, full wipe/reset/release/runback/re-entry/resurrection/ready-check order, and script-versus-every-bot decisions. Publish, diagnose, remote-verify, and exactly evict the batch. Six parallel BWD boss shards remain downstream of a passing Phase 1 gate.

## 2026-08-13T04:42:05Z — Future-encounter offensive authority approved; exact rebuild next

The sixteenth `9a3afe4defb748a654c03d8adaebe6d7295a19a1` uncapped capture live-proved the completed-admission `RaidRuntime` refresh, including exact all-dead and native wipe/reset generation observation. It also retained the first illegal decision chain: Hunter GUID 1279 submitted Multi-Shot 2643 at corridor Drudge GUID 60 immediately before Magmaw entered cohort threat and native boss state became `IN_PROGRESS`; Fire Mage GUID 1276 then submitted Living Bomb 44457 directly to Magmaw while route generation 3/index 2 still owned the Drudge pack. Repeated native resets changed counters without durable route or recovery progress and prevented the old watchdog from declaring a semantic stall. The batch remains an `infrastructure_abort`, not gameplay or fidelity evidence.

Exact clean checkpoint `53840bd89e3209905eaf82a4d57b5ed9668cae2c` closes every mandatory-review counterexample. Immediate-next boss and declared-add entries are frozen per owner before a route decision. Direct targets and all hostile multi-target offense are rejected across the profile resolver, common executor, direct combat/friendly helpers, `UnitAI`, `PetAI`, active `TotemAI`, passive/secondary totem spells, auto-attacks, and controlled-unit dispatch. This deliberately rejects multi-target actions wholesale while a protected encounter is next rather than estimating native chain/radius/trigger reach. Once contamination is linked, bot, pet, controlled, and totem offense remains held until native reset; teardown clears all owner authority. The uncapped watchdog now treats aggregate booleans and lifecycle counters as non-progress and advances only once for an exact-roster, wipe/attempt/assignment-bound, strictly ordered per-member death→corpse→release→runback→re-entry→resurrection proof followed by the full native ready check.

Mandatory independent `gpt-5.6-sol` high re-review found zero Critical, High, or Medium issues and explicitly authorized an exact coordinated build; live restart remains held pending that receipt and fresh preparation. The exact focused foundation/runtime/capture/research/pet-totem/Phase 9 partition passes 256 tests. The 9a3 eight-file object `c1359a231989f1fe00266948788a1c95.dir` was reverified remote-synchronized, then its 304,849,869-byte workspace and exactly eight child cache objects were removed while the tracked pointer, compact hashes, and `.dir` metadata were retained; no broad DVC garbage collection was used.

Exact next action: commit this status-only checkpoint, run the policy-v8 queued configure/incremental worldserver build from the exact reviewed descendant, then fresh reprovision/full verifier/v4 readback. Start and verify the single worldserver before attaching one Luna-xhigh Phase 1 babysitter. The next uncapped capture must clear the corridor without targeting protected Magmaw actors, then independently reconstruct native Magmaw engagement, full wipe/reset/release/runback/re-entry/resurrection/ready-check order and compare every bot decision with the native boss script. Publish, diagnose, and exactly evict the batch. Phase 1 and six parallel BWD boss shards remain downstream until that gate passes.

## 2026-08-13T04:05:00Z — Sixteenth capture proved runtime refresh and exposed future-boss splash authority

Exact `9a3afe4defb748a654c03d8adaebe6d7295a19a1` built successfully under policy v8. Gate-bearing configure/worldserver receipt hashes are `831d01865649bbc26fe41f882471908646a0cf3642077a7e4bbf023cb1620c56` and `314b0a3629f6d800ef8695dea01ad190f152998c6d024529d0d33f56fb0c57d4`; binary SHA-256 is `8b3c6114f13a71fd4b2c8ad6918c0f740419126e2f79fb0e8d615154680f6f5b`. Fresh provisioning, full verification, and v4 readback passed before the verified worldserver started and the Luna-xhigh babysitter attached.

The uncapped BWD 10N run proved the admitted runtime refresh live: `alive_size` moved from ten to zero and authoritative wipe/reset generations advanced. It also exposed a new decision-authority defect while route generation 3/index 2 still owned the Drakonid corridor. Hunter GUID 1279 submitted Multi-Shot 2643 at Drudge GUID 60 immediately before Magmaw GUID 39 first appeared in the cohort threat set and boss state became `IN_PROGRESS`. Fire Mage GUID 1276 then submitted Living Bomb 44457 directly to Magmaw while the declared route target remained Drudge 60. A later replacement Magmaw GUID 404 repeated the contamination. The route stayed fixed with three kills, while standalone boss-reset generation churn reached five and prevented the semantic-stall clock from expiring. Root stopped the diagnosed loop cleanly; all 496 rows bound with zero rejected/unchecked rows, no forbidden assistance occurred, worldserver exited zero, and cleanup proved zero bots/leases. This is an `infrastructure_abort`, not a gameplay or fidelity result.

The exact eight-file batch is remote-synchronized at `artifacts/cata_raid_program/phase1_raid_foundation_9a3afe4def_20260813.dvc`, object `c1359a231989f1fe00266948788a1c95.dir`, 304,849,869 bytes. Compact hashes and the first illegal action sequence are retained in `cata_raid_phase1_infrastructure_capture_9a3afe4def_summary_v1.json`. The remediation centralizes immediate-next encounter identity, rejects direct/multidot selection and native-data-derived chain/area reach into that encounter, holds every offensive source once premature linkage is observed, and stops treating wipe/reset counters alone as forward progress while retaining independently observed native recovery milestones. The focused Phase 1/runtime/Phase 9 suite passes 104 tests. Mandatory exact Sol-high review is next; only a zero-Critical/High result authorizes the exact build. After the diagnosis is consumed and remote state reverified, exactly evict this workspace and its eight child cache objects while retaining the pointer and `.dir` metadata. Phase 1 and parallel boss shards remain blocked.

## 2026-08-13T03:31:17Z — Fifteenth capture reached Magmaw; admitted runtime refresh approved

Exact reviewed checkpoint `a73ed7eaf56d53bab12e9ef44293c1ba54b66508` built successfully under policy v8 with gate-bearing configure/worldserver receipt hashes `f4a6bad047271cb0dc412579967929ec16367c5e9da4a8fe48a476cd410fe49e` and `74190cce4aecd8b54710fa6f9731450efb513a310fd8189485b13a83b85f33c6`; binary SHA-256 was `bc9396005e40a5cbf0173f9b0818cbb79439308981b32740937f2a51ce28073f`. Fresh provisioning, the full verifier, and v4 readback passed before a verified worldserver was started and the Luna-xhigh babysitter attached.

The fifteenth uncapped capture formed exact BWD 10N instance/save 2, killed all five prerequisite targets, reached Magmaw, observed native target/threat/add actors, and then observed every permanent bot dead. It stopped after 304.121 seconds because `RaidRuntime` remained frozen at admission: `alive_size=10`, no encounter, evidence sequence, wipe/reset, or recovery generations. Per-bot decisions and cumulative death metrics continued, proving the manager update loop was alive, but the completed-admission branch returned without refreshing the group runtime. This is an `infrastructure_abort`, not a gameplay or fidelity result.

Checkpoint `b50e3f47e25a569e7f9796dfc164b53eae35c318` refreshes `RaidRuntime` after every completed-admission exact-identity pass. Before the refresh, it now proves the ten expected manifest GUIDs, exact native member count, no foreign member, and every immutable subgroup; drift reaches the existing atomic cleanup and terminal latch. Native far-worldport members retain their slots and the refresh continues to defer while formation is incomplete. Mandatory independent Sol-high review found zero Critical, High, or Medium issues and authorized the exact coordinated build; the focused Phase 1/Stonecore partition passes 237 tests.

The eight-file a73 batch is remote-synchronized at `artifacts/cata_raid_program/phase1_raid_foundation_a73ed7eaf5_20260813.dvc`, object `7ecdd4cc58ed4d625ac4252e584f76d2.dir`, 220,738,552 bytes. After diagnosis and review, its workspace and eight exact child cache objects were removed while the pointer, directory metadata, and compact hashes remain; no broad DVC garbage collection was used. Exact next gate is the coordinated build of the reviewed b50 checkpoint, fresh reprovision/full verifier/v4 readback, then another uncapped capture with the worldserver verified before Luna-xhigh babysitting. Phase 1 and parallel boss shards remain blocked until the native Magmaw wipe/reset/recovery smoke passes.

## 2026-08-13T02:20:57Z — Fourteenth capture exposed partial-retreat ordering; viable live-pack remediation checkpointed

Mandatory Sol-high review approved exact `476ec4c06bc88ac82a54423f60f41998de887fc2`. Its policy-v8 configure and worldserver builds succeeded with gate-bearing canonical receipt hashes `47b57e07af7e0cb2aff26aab0154a504610d57a8b831c5138391ec23cf67a503` and `f8f3ebdbb751c0c537d5cecedb463b1ecc245a6b2bf7322b26f9f85550cdc0ee`; binary SHA-256 is `f536bca123d449514672577db09988ae249ffc0cebf510925c8be3124e5d7657`. Fresh provisioning, full verification, and v4 readback passed for the exact offline/unleased BWD 10N roster before the worldserver was started and verified. Only then did the Luna-xhigh babysitter attach.

The fourteenth uncapped capture formed the exact 2/3/5 raid in BWD 10N instance/save 2, retained a clean identity, and killed both entry targets plus the Chainwielder. All 343 retained rows were independently bound with zero rejected or unchecked rows. No forbidden assistance occurred, the worldserver exited zero, and cleanup proved zero bots and leases. Magmaw was not engaged, so this remains an `infrastructure_abort` with no gameplay or fidelity inference.

The previous safe-memory split was no longer the terminal cause. Both exact current-generation Drudges, runtime GUIDs 59 and 60, remained alive, attackable, formation-linked, and combat-linked; GUID 59 reached 17.7143% health. Native 15-yard minimum-distance handling recorded 12 started exits and zero failures. After the main tank and two DPS died, however, the generic critical-role partial-wipe branch selected `tactical_retreat_no_combat_res_previous_route_anchor` before live-pack reengagement. The living off-tank, three healers, and remaining DPS therefore dragged the two native Drudges backward instead of clearing the current pack or entering a coherent native reset. Semantic progress stopped after 305.443 seconds and 57 unchanged samples.

Mandatory Sol-high review rejected the first intermediate checkpoint with three High findings: it still used the currently ticking bot's target/path, allowed mutable or foreign group roles to contribute viability, and the tracked full SHA was mistyped. Exact checkpoint `f12976832c431b740bae682a3737700e6dba33ee` closes all three in source and tracked identity. The continuation decision now sorts the frozen living tanks and exact pack GUIDs, uses one shared native combat-link/attackability/strict-tank-path result, and derives tank/healer/DPS only from exact active leased `Raid.RosterByGuid` slots in the original instance. A scattered healer's local path, an unleased foreign group member, or mutable LFG role cannot change the cohort decision. Stale/dead/transition packs and tank-only or otherwise nonviable compositions retain fail-closed tactical/native recovery. Native hazard and union-safe 15-yard rules, boss routes, Stonecore paths, and no-assistance semantics remain unchanged. The focused Phase 1/corridor/capture/generic-smoke/Phase 9 set passes 110 tests.

The exact eight-file batch is remote-synchronized at `artifacts/cata_raid_program/phase1_raid_foundation_476ec4c0_20260813.dvc`, object `d6493e778cd0a4d9c2489e247cd584e4.dir`, 196,857,068 bytes. Its compact classification and hashes are retained in `cata_raid_phase1_infrastructure_capture_476ec4c0_summary_v1.json`. Keep it hydrated only through mandatory review of the remediation, then exactly evict the workspace and eight child cache objects while retaining the pointer and directory metadata. No broad DVC garbage collection is allowed. Exact next gate is mandatory Sol-high review of `f12976832c431b740bae682a3737700e6dba33ee`, then an exact coordinated rebuild, fresh reprovision/full verifier/v4 readback, and another uncapped canonical capture with the verified worldserver running before the Luna-xhigh babysitter attaches. Phase 1 and parallel boss shards remain blocked until the native Magmaw wipe/reset/release/runback/reentry/resurrection/ready-check smoke passes.

## 2026-08-13T01:43:12Z — Thirteenth uncapped capture diagnosed; live-pack authority remediation checkpointed

Mandatory Sol-high review approved exact `c35ead7a33bb1aecd05347ea28d367e4e89596e1`, and its policy-v8 configure and worldserver builds succeeded with gate-bearing receipt canonical hashes `a2ec306684673adb284f348296de94ed67f6026bb2975cb78aa6d51afa16996f` and `5f30cf2c6475f18807801d63cfb078b9789f5495131869e5f0439f9b33510b9e`. The resulting binary SHA-256 was `a2c7e259822423b59b79c4b2615bf4e7f0805105f9a805101dcd8faaba097fea`. Fresh provisioning, the full verifier, and v4 readback passed for the exact offline/unleased BWD 10N roster before live start.

The thirteenth uncapped capture formed BWD 10N in instance/save 8 with the exact 2/3/5 roster. It killed the two entry targets and the Chainwielder, then stopped after 302.73 seconds and 55 unchanged samples at the Drakonid corridor. All 352 retained evidence rows bound to the frozen identity with zero rejected or unchecked rows; forbidden assistance was absent and cleanup proved zero bots, leases, capture writers, and worldserver processes. Magmaw was not engaged, and no native boss wipe/reset/recovery/ready-check sequence was observed, so the run is an `infrastructure_abort`, not a gameplay or fidelity result.

Root and the Luna-xhigh babysitter independently reconstructed the terminal bot-decision defect. Both entry-42362 Drudges remained exact current-generation live-pack members after recovery. Their native spell 79604 has a 15-yard damaging radius, and the run contained both successful and failed union-safe exits; that mechanic and its strict path safety are retained. The actual terminal split began when leader GUID 1271 installed `validation_route_safe_memory_after_death_loop` and repeatedly moved to `(-339.765,-316.824,212.549)` while the other tank followed the canonical corridor authority near `(-328.403,-88.036,213.921)`. Endpoint readiness then correctly held on `trash_cluster_cohort_not_at_endpoint` until the semantic-stall monitor stopped the run.

Exact source checkpoint `c774f691e96c8e5afac86b957230ed7471d027af` gives current-generation persisted live-pack identity precedence over only that generic remembered safe position. It clears an already-installed safe-memory override and blocks a new one while an exact non-dead/non-transition pack member remains; generation changes, actual deaths/transitions, partial-wipe rendezvous, live-pack reapproach, the native 15-yard rule, union-safe paths, identity, and no-assistance gates remain unchanged. The focused Phase 1, corridor, generic-smoke, capture, and inherited Phase 9 partition passes 107 tests. Exact next gate is mandatory independent Sol-high review of `c774f691e9`, followed by an exact coordinated policy-v8 rebuild, fresh reprovision/full verifier/v4 readback, and another uncapped capture with the verified worldserver running before a Luna-xhigh babysitter attaches.

The exact eight-file thirteenth batch is remote-synchronized at `artifacts/cata_raid_program/phase1_raid_foundation_c35ead7a_20260813.dvc`, object `6f8705a8f3d3bc5f2fc8a36339a0191d.dir`, 192,468,543 bytes. It remains locally retained only until this remediation and its mandatory review have consumed the diagnosis, after which it must be exactly evicted. The older fae batch was remotely verified and exactly evicted, retaining its pointer, compact hashes, and directory metadata. No broad DVC garbage collection was used. Phase 1 remains false, and parallel boss shards remain downstream of this gate.

## 2026-08-13T02:56:27+03:00 — Exact native-full-wipe/no-assistance remediation ready for mandatory review

Exact source checkpoint `680e5e2da424cfffcb86e2d546e50d93e7077fb5` closes the two eleventh-capture defects without weakening ordinary raid behavior. The Magmaw Phase 1 route now carries a typed `native_full_wipe_only` recovery policy. Partial critical-role deaths no longer trigger tactical retreat, and both party resurrection and class self-resurrection are held only for this non-fidelity smoke so every permanent GUID must traverse the native corpse release/runback/re-entry path. Once `RaidRuntime` observes the exact all-dead roster it latches attempt, wipe generation, roster, and per-GUID death identity; the first ghost leaving BWD cannot revoke that authority and deadlock the other nine releases.

Validation raid boss recovery now returns fail-closed before `Creature::Respawn`, `Map::Respawn`, or `Creature::LoadFromDB`; those legacy helpers remain scoped to non-raid routes. No direct boss state, spawn, resurrection, or teleport is manufactured. The previously reviewed Magmaw script reset/body/death lifecycle remains unchanged and approved at the parent; this checkpoint still makes no Magmaw tactics, kill, or 4.4.2 fidelity claim.

The regenerated `validation_scenarios` object is `4026b34e6c368f236d7975acf166873a.dir`, five files/127,112 bytes, with `boss_recovery_policy:native_full_wipe_only` on the exact Magmaw node. It is pushed and local/cloud status is in sync. After the remediation consumed the eleventh capture diagnosis, its 219 MiB workspace payload, two temporary run directories, and exact eight child cache objects were removed; the remote object, tracked pointer, compact hashes, and tiny directory metadata remain recoverable, with no broad DVC garbage collection. The focused foundation, runtime, research, capture, coordinator, and inherited Phase 9 partition passes 258 tests; Python compilation and diff checks pass. The exact next gate is mandatory independent Sol-high review of `680e5e2da4`, then an exact coordinated policy-v8 rebuild, fresh reprovision/full verifier/v4 readback, and another uncapped capture with the verified worldserver running before the Luna-xhigh babysitter attaches. Phase 1 remains false until live native engagement, full wipe/reset/release/runback/re-entry/resurrection/ready-check ordering and immutable DVC evidence pass.

## 2026-08-13T02:42:00+03:00 — Eleventh uncapped capture exposed partial-retreat and forced-respawn assistance loop

Mandatory Sol-high re-review approved exact clean `af3d5229bf6f00f9b4c84632ce758e83c2607231` with zero Critical/High/Medium findings. Policy-v8 configure and worldserver builds then succeeded from that exact source with gate-bearing receipt SHA-256 values `df8c6f39d52cb604b9b85a73385562a26a277cf1d6602821ca690651e30cd143` and `b03f8390077ce07afadd77a2cadf975acff7d4e2d42b90871aa650dceab34c97`; binary SHA-256 is `9bc453f0257ae5bcac15b659a4473f5785e171715a173eb20a235d3ef6350d27`. Fresh authoritative provisioning, the full verifier, and v4 database readback passed before live start, proving the exact frozen 2/3/5 roster offline and unleased on map 669 with zero instance, group, ghost-aura, corpse, or corpse-phase residue.

The eleventh uncapped capture formed BWD 10N in instance/save 8 and again killed both entry targets plus the exact Chainwielder/two-Drudge corridor. It reached route generation 4/index 3 at Magmaw. The bots selected entry 41570 under typed authority and emitted tank-position, single-target, boss-healing, and native ground-danger decisions. Undeclared nearby targets were rejected. This is useful infrastructure evidence, but it is not a boss-mechanic or fidelity pass.

The run never retained a stable native encounter state. Individual critical-role deaths caused partial-wipe tactical retreat, native release/runback decisions, and regrouping before the exact full wipe required by Phase 1. The route's canonical boss recovery then directly invoked `Creature::Respawn`, `Map::Respawn`, or `Creature::LoadFromDB`, producing `canonical_boss_scheduled_respawn_advanced` and another short pull. That is forbidden assistance under the approved raid plan even though the normalized assistance scanner did not classify the internal result marker. No native wipe/reset/recovery/ready-check sequence was accepted, and no gameplay or 4.4.2 fidelity inference is authorized.

The monotonic semantic monitor correctly stopped the uncapped run after 304.073 seconds and 46 unchanged samples. Final high water was five kills, sixteen cumulative deaths, zero boss kills, zero boss/reset/wipe/recovery generations, and encounter phase `formation`. Cleanup succeeded with zero bots, leases, and worldserver/capture processes. Luna-xhigh babysitting independently confirmed the typed entry-41570 decisions, repeated target loss/retreat, and absent durable script lifecycle.

The exact eight-file batch is pushed and remote-verified at `artifacts/cata_raid_program/phase1_raid_foundation_af3d5229_20260813.dvc`, object `1f06df4d0ade61f77084cf1401f2821a.dir`, 229,474,387 bytes. Compact hashes and the independent assistance override are retained in `cata_raid_phase1_infrastructure_capture_af3d5229_summary_v1.json`. Exact next gate is scoped native-full-wipe/no-forced-respawn remediation, mandatory independent Sol-high review, exact coordinated rebuild, fresh reprovision/full-verifier/v4-readback, and another uncapped canonical run. Parallel six-boss shards remain downstream of a passing Phase 1 gate.

## 2026-08-13T01:48:00Z — Tenth uncapped capture live-cleared the Drakonid corridor and isolated Magmaw pre-pull lifecycle

Mandatory Sol-high review approved exact `291653c1549d972c0f363e9505e30e3462db5e31` with zero Critical/High/Medium blockers. Policy-v8 configure and worldserver jobs succeeded from that exact clean source with gate-bearing receipt SHA-256 values `69f9f81651a038bfd75ef7a51eeccd652270e1fa1c5217eef2f6cc74a3f3827c` and `2326ae5a016c2556ef59c0d8d4dbdc0f18cb1b59bab07f45d9cd5f29b84a5e4a`; binary SHA-256 is `98973ab1a85022a67d39378e08dad59ccd00778d9eb02ed07cd5d27a55796417`. Fresh full provisioning verification and v4 readback proved the exact offline/unleased ten with zero instance, group, ghost-aura, corpse, or corpse-phase residue before live start.

The tenth uncapped capture formed BWD 10N in instance/save 8, killed both entry mobs, then killed the exact Chainwielder and two Drudges. Trace reconstruction observed 173 native hazard exits plus deterministic union-safe minimum-distance movement against both Drudges. This is the first live clear of the complete declared corridor and validates the bounded pack/threat/hazard decision remediation for Phase 1 infrastructure scope.

At the Magmaw node, target authority remained correctly bound to entry 41570, but every short pull used a replacement runtime GUID, disappeared without a sampled `IN_PROGRESS` boss state, and produced partial deaths/retreat/release/runback before the cycle repeated. The native Magmaw script checks four summoned body parts on engagement; when one is missing it calls evade but incorrectly continues into `BossAI::JustEngagedWith` and event scheduling. This is consistent with the observed brief target/death/disappear cycle, though the exact missing body-part mask was not present in the retained telemetry and must be directly proven on the rerun. The monitor also counted cumulative deaths and replacement runtime GUIDs as forward progress, so root terminated the already-invalid cycle after 34 deaths. This is an infrastructure abort, not a gameplay failure or fidelity result.

Luna-xhigh babysitting independently confirmed correct entry-41570 target selection, zero durable Magmaw engagement/reset/recovery evidence, and the repeated pre-pull failure. All 635 retained rows were identity-bound with zero rejected or unchecked telemetry rows and no forbidden assistance. External termination prevented the normal stop/cleanup row, so the next authoritative reprovision/readback must independently clear leases, group/instance residue and corpses before another live start.

The exact eight-file batch is remote-synchronized at `artifacts/cata_raid_program/phase1_raid_foundation_291653c1_20260813.dvc`, object `66eb70cf3b8ad97ed29561d9df7c0119.dir`, 355,398,346 bytes. Compact hashes and classification are retained in `cata_raid_phase1_infrastructure_capture_291653c1_summary_v1.json`. Exact next gate is source remediation for Magmaw body ownership/engagement fail-close, stable-entry monotonic stall accounting, and position/recovery diagnostics; then mandatory independent Sol-high review, exact rebuild, fresh reprovision/full-verifier/v4-readback, and uncapped rerun. Parallel boss shards remain downstream of a passing Phase 1 gate.

Checkpoint `0a3d8b95a8` implements that remediation. All four Magmaw body parts now use explicit manual summon lifetime, an incomplete set is atomically rebuilt once at native engagement, and persistent incompleteness logs the exact mask then evades and returns before `BossAI::JustEngagedWith` or event scheduling. Bot diagnosis now includes exact map/instance/position/alive/ghost/corpse/in-world state. The uncapped monitor excludes cumulative deaths and keys target-health high-water marks by stable creature entry, so replacement runtime GUIDs cannot mask a recurring pull failure. The expanded Phase 1/research/coordinator/Phase 9 partition passes 251 tests and Python compilation/diff checks pass. Mandatory exact Sol-high review is pending; the 291 binary is intentionally stale for this source.

The mandatory review rejected exact `5ead547e17` on four lifecycle gaps, and checkpoint `cb1b2ce919` closes them without starting a build. Reset now clears pull counters, exposed-head and heroic-phase state, restores passive react state, and disposes any remembered body. Body creation is all-four-or-none and returns the precise failed-bit mask; engagement requires every part alive, in-world, and attached to Magmaw. Native death explicitly owns every manual part, including the real exposed head omitted from `SummonList`. The uncapped monitor advances once for each newly observed encounter phase, while phase oscillation, deaths, and replacement GUIDs cannot extend the run indefinitely. The expanded partition again passes 251 tests. Exact descendant re-review is pending and build/live remain held.

## 2026-08-12T21:56:48Z — Ninth uncapped capture isolated Drakonid tactics; explicit native pack decisions checkpointed

Mandatory Sol-high review approved exact `e75f5ba2fd63646e9ebe432ad59987c76aababab`. Policy-v8 configure and worldserver jobs then succeeded from that exact clean source with gate-bearing trusted-local receipt SHA-256 values `5c451d3503a9e3d30993d23fb8a5c69d34f8f805d8c576cf64dd33b76872d3ed` and `f053d8dc440a596fb38c2ae87c51a9009be23786fc7533f424f1bf3fc66e58aa`; binary SHA-256 is `a8a7a586a512d3dd3b09c7758f4bc3149a7741e95707fb0dad273e93e535729a`. Authoritative provisioning, the full verifier, and v4 readback again proved the exact ten offline/unleased characters with zero instance, group, ghost-aura, corpse, or corpse-phase residue.

The ninth uncapped capture formed the exact BWD 10N 2/3/5 roster in instance/save 2, killed both entry-trash targets, and preserved immutable admission identity through repeated deaths. This live-proves that the exact native-recovery worldport exception no longer triggers the eighth run's premature teardown. All 379 retained rows were identity-bound with zero rejected or unchecked rows, assistance was absent, and cleanup proved zero bots, leases, and processes. No accepted full-wipe recovery or ready check occurred, and Magmaw was not engaged, so the run remains an infrastructure diagnosis with no gameplay, boss-mechanic, or 4.4.2 fidelity claim.

Luna-xhigh babysitting and root reconstruction isolated the next defect. While the route was already on the Magmaw boss node, generic prerequisite logic repeatedly pulled one Drakonid Chainwielder (entry 42649, spawn 250050) and two Drakonid Drudges (entry 42362, spawns 250140/250141) that the boss contract did not declare. Both tanks owned the three hostiles, but the inherited dungeon threshold demanded 2000 absolute threat and 2.5x headroom, withholding most raid damage. Ranged and healers stacked inside native Drudge spell 79604's 15-yard radius, and no decision contract existed for the Chainwielder's native 79578/79580 Overhead Smash marker (entry 42690, 20-yard radius). Critical-role deaths triggered tactical retreat, the pack reset, and the loop repeated until 303.607 seconds of monotonic semantic stall.

Checkpoint `a662679d1a41624ce19ef83ef74846f76a779424` inserts a bounded route-owned Drakonid corridor trash node before Magmaw with exact entries, source identities, 48-yard cluster envelope, native Overhead Smash geometry, and a 15-yard Drudge minimum-distance rule. Ranged/healer assignments execute a path-validated exterior move; the generic hazard executor owns exact radial exits. Declared raid trash now releases damage only after current tank victim ownership plus the core ThreatManager's 1.3 ranged switch headroom, recomputed every decision, while legacy dungeon tuning remains unchanged. Boss nodes reject undeclared hostiles instead of executing generic prerequisite damage. The generated route is now ten ordered nodes at remote-synchronized DVC object `e79e1997288e7b21ae82473ccfa9522c.dir`; the focused foundation/research/capture/Phase 9 suite passes 219 tests.

The ninth eight-file batch is remote-synchronized at `artifacts/cata_raid_program/phase1_raid_foundation_e75f5ba2_20260813.dvc`, object `679597e1371aecd365c79e8e77dcae94.dir`, 212,449,797 bytes. After diagnosis and code checkpoint, its workspace payload, temporary directory, and exact eight child cache objects were removed; pointer/directory metadata and the compact hash/classification summary remain, and no broad DVC garbage collection was used.

Exact next gate is mandatory independent `gpt-5.6-sol` high review of the status descendant of `a662679d1a`. Zero unresolved Critical/High findings are required before the exact coordinated rebuild, reprovision/full-verifier/v4-readback gate, and next uncapped capture. That rerun must verify the declared pack boundary, 79580 and 79604 decisions, threat release, native recovery ordering, Magmaw engagement/script events, and each bot's actual decisions. Parallel boss shards remain downstream of Phase 1: root prestarts and verifies the single worldserver and every boss-ready shard before one Luna-xhigh babysitter per boss begins; final qualification remains three sequential full clears and a fourth on material disagreement.

## 2026-08-12T21:06:49Z — Eighth uncapped capture live-proved the BWD junction; exact native-recovery admission remediation checkpointed

Mandatory Sol-high review approved exact `9ae33c0d408d515a3bebed0c38b2a5698bef0782`. Policy-v8 configure and incremental worldserver jobs then succeeded from that exact clean source with gate-bearing trusted-local receipt SHA-256 values `7541d8c9ec49f731781156b7d2dafe7f004c77289c7f14ec0dfb84b55ec44165` and `941c38452395ce6d77b321513efad1ed8f0ceb7e66769f5012226b8ec557fa9b`; binary SHA-256 is `5e098b64289ab691e5888b81a024bade125bcfa86df22c870ad01900b58cb5f7`. The focused suite passed 247 tests. Authoritative provisioning, the full verifier, and v4 readback again proved the exact ten offline/unleased characters with zero instance, group, ghost-aura, corpse, or corpse-phase residue.

The eighth uncapped capture formed the exact BWD 10N 2/3/5 roster in instance/save 2, killed both declared entry-trash targets, reached the inserted junction regroup, and advanced to route generation 3 at the Magmaw node. This live-proves the new native junction path. Before Magmaw engagement, native prerequisite corridor combat produced nine deaths. Between the last exact ten-member telemetry sample and the next poll, the completed-admission guard atomically removed all members with `validation_raid_admission_identity_drift`. No Magmaw engagement occurred, so this run makes no boss-script, tactics, gameplay, or 4.4.2 fidelity claim.

Read-only Luna-xhigh diagnosis ranked a legitimate native recovery far-worldport racing the admission guard as the most likely cause: admission checked `IsInWorld()` before `TryReattachValidationBot` could acknowledge either the exact native ReleaseSpirit transfer or canonical BWD entrance return. The capture did not expose the culprit member or far-worldport state, so that member-level fact is explicitly not claimed as observed. Checkpoint `372c250bb6d0f68790d44b93084913f23f3fa6ff` keeps exact GUID/slot/lease/group/leader/map/instance checks and permits only those two existing corpse-, destination-, and teleport-type-bound native transitions. Missing players/groups, wrong destinations, ordinary teleports, and every immutable identity mismatch still fail closed. Future failures log the exact predicate and member GUID.

The watchdog stopped after 301.684 seconds without monotonic progress. Cleanup proved zero bots, leases, and worldserver processes; forbidden assistance was absent. The eight-file batch is remote-synchronized at `artifacts/cata_raid_program/phase1_raid_foundation_9ae33c0d_20260812.dvc`, object `af88aa48da4b3e9a4130ca56395490e9.dir`, 68,881,810 bytes. Its workspace payload, exact eight child cache objects, and both diagnosed temporary capture directories were removed after compact retention; the pointer/directory metadata remain recoverable and no broad DVC garbage collection was used.

Exact next gate is mandatory independent `gpt-5.6-sol` high review of the status descendant of `372c250bb6`. Zero unresolved Critical/High findings are required before the exact coordinated rebuild, reprovision/full-verifier/v4-readback gate, and next uncapped capture. That rerun must assess the prerequisite corridor pull, native recovery ordering, Magmaw script events, and each bot's actual decisions. Parallel boss shards remain downstream of Phase 1: root prestarts and verifies the single worldserver and every boss-ready shard before one Luna-xhigh babysitter per boss begins; final qualification remains three sequential full clears and a fourth on material disagreement.

## 2026-08-12T20:29:00Z — Seventh uncapped capture isolated the BWD path-length gate; native entrance-junction route published

Mandatory Sol-high review approved exact `552974620bc2b1aec1a2e067a61a38e42a499f5c` for build. Policy-v8 configure and worldserver jobs then succeeded from that exact clean commit with gate-bearing trusted-local receipt SHA-256 values `758a7bab6909bd809564db4e5f9042b474c6e3902e3aa3021a825ecb8b840dad` and `05261d0ac6ce0a3f42f341f77725933503ec8c1efbb11441233b29070c739807`; the resulting binary SHA-256 is `cb0037117b6a4bf36873e144c4b7e6e30fce9ec7e2aa1bc002edaf1d7ead0669`. Authoritative provisioning, the full live verifier, and v4 readback passed with all ten characters offline/unleased at the exact map-669 start and zero instance, group, ghost-aura, corpse, or corpse-phase residue.

The seventh uncapped capture formed the exact BWD 10N 2/3/5 roster in instance/save 2, preserved immutable build/profile/group/roster identity, and killed both entry-trash targets without deaths. It then stopped after 305.032 seconds of monotonic semantic stall before engaging Magmaw. All ten bots repeatedly and correctly rejected the direct route from approximately `(-339,-338,214)` to the Magmaw anchor `(-307.531,-35.4375,211.815)` as `route_destination_unreachable`; no teleport, shortcut, partial-path, or off-mesh fallback was used. All 262 retained rows were identity-bound, the 86 diagnosis and 86 trace envelopes were complete, assistance was absent, and cleanup proved zero bots, leases, and processes. This is an `infrastructure_abort`, not gameplay or 4.4.2 fidelity evidence.

The exact engine limit explains the failure: `PathGenerator.h` permits only 74 four-yard smooth-path points, or 296 yards, while the failed straight-line leg is already about 304 yards before navigating the BWD geometry. Checkpoint `c3007076a1f8856a59f3bf7dc6ed5af619d79e0a` inserts one deterministic regroup at the exact frozen entrance start `(-345.872,-224.344,193.127)`, which the successful outbound trash path and provisioning contract already prove. The resulting native legs are approximately 125 and 193 yards. No door state, encounter state, boss, helper actor, teleport, or guessed waypoint is changed. The regenerated nine-node scenario object `64e25ae86ef14973f49079de5beca189.dir` is remote-synchronized. Focused tests pass 215 with one inherited stale Stonecore structural failure in the legacy diagnostic; coordinator tests pass 32.

The eight-file capture batch is remotely synchronized at `artifacts/cata_raid_program/phase1_raid_foundation_55297462_20260812.dvc`, object `7dc7a8b9ad5ea3020e786ba412f416fa.dir`, 131,676,086 bytes. Its workspace payload and exact eight child cache objects were evicted after targeted push and remote verification; directory metadata, pointer, and compact summary remain. No broad DVC garbage collection was used.

Exact next gate is mandatory independent `gpt-5.6-sol` high review of `c3007076a1`. Only zero unresolved Critical/High findings authorize fast-forwarding the isolated worktree, an exact coordinated rebuild, repeat provisioning/v4 readback, and the next uncapped canonical Phase 1 run. Parallel boss shards remain downstream: root starts and verifies the one worldserver and every disjoint boss-ready shard before one Luna-xhigh babysitter per boss begins; final qualification remains 3–4 sequential full-raid runs.

## 2026-08-12T19:45:27Z — Sixth uncapped capture diagnosed; persisted recovery-state admission remediated and independently reprovisioned

Mandatory Sol-high review approved exact `afeb52c7118361a04fbc99fd16629b39d90a9316` with zero Critical/High findings. Exact policy-v8 configure and worldserver build then succeeded with gate-bearing trusted-local receipts: configure SHA-256 `12b1cff8a42ff9736759d103330beb188baa8b44a1697e331903dbadd0e87c19`, worldserver SHA-256 `eb00e7e6df924ef7881e9f20b5fd77c061b708804542d375d67c0fd58833bbc8`, and binary SHA-256 `3e305f7cc2edd09004b815466cd04c830bf6a8ae073c0dbd70babcdef2bb8afd`. The exact post-build partitions passed `213 + 32` tests, authoritative provisioning and its 50-character live verifier passed, and the earlier v2 BWD readback appeared clean because it did not inspect persisted recovery state.

The sixth uncapped capture formed the exact BWD 10N 2/3/5 roster in instance/save 2, preserved immutable identity, and killed both entry-trash objectives. It stopped cleanly after 303.216 seconds without monotonic progress. Diagnosis proved `Bwddpsa` was already a ghost before autonomous combat: the first status reported nine alive at duration zero, while its death trace preceded the later trash kills. The database retained `PLAYER_FLAGS_GHOST`, aura 8326, and a BWD corpse from the prior run. Native recovery correctly rejected that stale partial-death chain because it was neither an observed alive-to-dead transition nor a full-wipe-scoped release/runback sequence. This is an `infrastructure_abort`, not gameplay or fidelity evidence. All 244 rows were identity-bound, all 80 diagnosis and 80 trace envelopes were complete, no assistance occurred, and cleanup proved zero bots, leases, and processes.

The seven-file immutable batch includes both exact build receipts and provisioning proofs at `artifacts/cata_raid_program/phase1_raid_foundation_afeb52c7_20260812.dvc`, object `7a90a2d90b99f211c2763e6840764b65.dir`, 120,150,033 bytes. After targeted push and remote verification, the workspace payload and exact seven child cache objects were evicted while directory metadata and `cata_raid_phase1_infrastructure_capture_afeb52c7_summary_v1.json` remain. No broad DVC garbage collection was used.

Implementation checkpoint `508368a5e3` closes the contamination path. Deterministic provisioning and route reset now clear corpse phases, corpses, ghost aura 8326, ghost and resurrect-at-login flags, stale character-instance rows, and restore full stat seeds. Before any lease, login, spawn, or group mutation, C++ independently requires every exact permanent roster member to have full persisted seeds and zero recovery residue. Final admission also requires every loaded member alive, non-ghost, corpse-free, and bound to the exact frozen map and positive instance; failure performs the existing atomic rollback. Reapplication executed 50 account and 5,768 character statements. The full live provisioning verifier passes, and readback v3 independently proves all ten BWD characters offline/unleased at map 669 with zero instance/group/corpse/ghost residue. The focused gates pass `214 + 32`; regenerated provisioning DVC objects are pushed and remotely synchronized.

Exact next gate is mandatory independent `gpt-5.6-sol` high review of the clean status descendant of `508368a5e3`. Only zero unresolved Critical/High findings authorize a fresh exact coordinated build, repeat v3 reprovision/readback, and seventh uncapped canonical Phase 1 capture. Parallel boss shards remain downstream: root prestarts and verifies the one worldserver and all disjoint boss-ready identities before one Luna-xhigh babysitter per boss begins. Final qualification remains 3–4 sequential full-raid runs.

## 2026-08-12T19:05:00Z — Exact 82f build and fifth uncapped capture diagnosed; partial-death corpse authority and full-stat provisioning remediated

Mandatory Sol-high review approved exact `82fce3fc13ba4ebc73fa4fe919a68c31094d718d` with zero Critical/High findings. Exact policy-v8 configure and worldserver build then succeeded with gate-bearing trusted-local receipt SHA-256 `7ad317dde4555b19c578c2d20d295efc7b6efc4b9e5e94a59838bb297087d45e` and binary SHA-256 `c86fe2f44be0e75131cb76f428dbb4cef44920e66d0fc1b601f89b61adfa9b7b`. Regression partitions passed `211 + 32`; the refreshed provisioning verifier passed all 50 accounts/characters, payload, equipment, spell, talent and glyph facts, and the exact BWD roster readback passed only after stale instance residue was removed.

The fifth uncapped capture formed the exact BWD 10N 2/3/5 roster in instance/save 2 with stable build/profile/group/roster identity. `Bwddpsa` died 264 ms after spawn because generated validation characters were persisted with only 100 health/power. The nine survivors correctly killed both declared entry-trash targets and held the cleared anchor. Native party resurrection selected the dead Player before core `BuildPlayerRepop` had created an authoritative corpse, repeatedly received `SPELL_FAILED_BAD_TARGETS (13)`, and starved the native release path; the dead bot then reported `native_corpse_authority_missing`. The watchdog stopped the run after 303.211 seconds without monotonic progress. This is an infrastructure abort, not gameplay or fidelity evidence. All 244 retained rows were identity-bound, diagnosis/trace envelopes were complete, assistance was absent, and cleanup proved zero bots/leases/processes.

The immutable three-file batch is remotely synchronized at `artifacts/cata_raid_program/phase1_raid_foundation_82fce3fc_20260812.dvc`, object `1c5a69b7f6ecec6ea9b834f499a79256.dir`, 120,590,593 bytes. After targeted remote verification, its workspace payload and exact three child cache objects were evicted; directory metadata and `cata_raid_phase1_infrastructure_capture_82fce3fc_summary_v1.json` remain. No broad DVC garbage collection was used.

The remediation keeps non-validation behavior unchanged. Raid-validation party resurrection now requires the existing exact corpse/map/instance/owner/group/leader authority before selecting a dead member; otherwise native release builds the corpse and proceeds through the reviewed BWD runback path. Provisioning and route-reset SQL now seed unsigned max health/power values, which native `Player::LoadFromDB` setters clamp to computed maxima. Readback v2 independently requires those persisted seeds. The provisioning/verification DVC stages were regenerated and pushed; focused gates pass `213 + 32`.

Exact next gate is mandatory independent `gpt-5.6-sol` high review of this final clean remediation checkpoint. Only zero unresolved Critical/High findings authorize another exact coordinated build, reapplication/readback of the regenerated provisioning, and the next uncapped canonical Phase 1 run. Parallel boss shards remain downstream of the shared Phase 1 live gate; root must prestart and fully verify the worldserver and all isolated shard identities before Luna-xhigh babysitters begin.

## 2026-08-12T17:48:39Z — Exact 9c9 build and fourth uncapped capture diagnosed; native BWD ghost worldport remediation ready for review

Mandatory Sol-high review approved exact `9c9dec8b8811cc3094ddebffd5aed3626d36f301` with zero Critical/High findings. Policy-v8 configure and one-job worldserver build then succeeded at that exact clean source. The gate-bearing trusted-local receipt binds binary SHA-256 `eed28b5aeafaac382a80a4f31115778de01a6c91323b7fda26e3ec1d0a48a0d5`; the expanded focused gate passes 242 tests and fresh provisioning readback independently proves the exact offline/unleased 2/3/5 BWD roster at map 669 with no group or instance residue.

The fourth uncapped canonical capture formed the exact ten-person group in BWD 10N instance/save 2, preserved profile/roster/group/difficulty identity, killed both entry-trash objectives, and recorded one native player death plus the native release-spirit opcode. It then stopped cleanly as `infrastructure_abort` after 303.563 seconds of objective stall: the released ghost never reached the outdoor graveyard/entrance path and repeatedly emitted `raid_entrance_area_trigger_missing`. Cleanup proved zero bots/leases, no worldserver process, 83 complete diagnosis and 83 complete trace envelopes, 253/253 identity-bound rows, and no forbidden assistance. Missing ready-check evidence is strictly downstream of never reaching a boss wipe; this is not a gameplay or fidelity result.

The first remediation hypothesis was superseded during mandatory adversarial review. Core `Player::TeleportTo` blocked every bot-session cross-map transfer from a dungeon, so native `RepopAtGraveyard` never created the expected worldport; additionally, map-0-only matching would not distinguish an unrelated pending transfer. The current remediation narrowly permits the core teleport only for a dead ghost with an exact owned corpse in the current dungeon instance and the exact `ObjectMgr`-resolved graveyard map/coordinates/orientation. Runtime independently matches that full pending destination, acknowledges it, walks to canonical BWD entrance trigger 6581 on map 0, and submits the native area-trigger opcode back to the exact corpse instance. The return ACK accepts only the core's native dungeon-corpse auto-resurrection with exact map/instance/group/leader postconditions and the corpse consumed. Arbitrary teleports, mismatched corpse/group/instance identity, unproven trigger data, and direct strategy teleport/resurrection assistance remain fail-closed.

The immutable three-file failure batch is remotely synchronized at `artifacts/cata_raid_program/phase1_raid_foundation_9c9dec8b_20260812.dvc`, object `e5ceb1891ab269ecacde6495321e7def.dir`, 126,958,523 bytes. Its exact workspace payload and three child cache objects were evicted after diagnosis and remote verification; directory metadata remains and no broad DVC garbage collection was used. Compact hashes and classification are retained in `cata_raid_phase1_infrastructure_capture_9c9dec8b_summary_v1.json`.

Exact next gate is mandatory independent `gpt-5.6-sol` high review of the status descendant of `c8acc065cd`. Only zero unresolved Critical/High findings authorize an exact policy-v8 incremental rebuild, the 243-test/readback gate, and another uncapped canonical Phase 1 run. Parallel one-boss/one-Luna-xhigh live babysitting remains downstream: root first starts the single worldserver and fully provisions, identity-verifies, partitions, and boss-readies every shard. Final acceptance still requires 3–4 sequential full raid clears. All 464 material fidelity rows remain fail-closed.

## 2026-08-12T16:59:54Z — Exact frozen-roster transaction and DB-backed evidence remediation ready for re-review

The mandatory Sol-high review of exact `278b3e5197` rejected it with zero Critical and four High findings: native failures after preflight could leave a partial cohort and retry unpinned, role-only selection could substitute another same-tag character for the permanent roster, typed shared-focus authority could independently retarget another boss, and provisioning evidence was not retained as a fresh DB-backed artifact. No build or live restart was admitted.

Implementation checkpoint `a9994c4aee496774fb71281e933241895e2919d8` closes all four in source and tests. Each of the eight generated BWD route rows now carries the same exact ten permanent slot/GUID/name/role/class-spec identities. Runtime queries that exact identity and validates every saved map/XY/Z position before mutation. Admission is a one-shot transaction: any late lease, native login, or exact-group failure removes every spawned bot, releases every claimed lease, restores party/raid/metrics, marks pool rows unused, and latches terminal failure so later ticks cannot use generic selection or fallback. Completed ticks independently reconstruct exact GUID-to-slot membership, per-slot leases, frozen roster rows, loaded/in-world membership, one native group, and frozen group identity; drift performs the same full cleanup before terminal failure.

The declared shared boss target is now passed directly into typed mechanic authority. It cannot be replaced by a bot victim, group victim, or unrelated nearby boss, and an undeclared bound target fails closed. Capture taxonomy now classifies nonzero worldserver exit, Git identity loss, evidence demux/capture loss, and telemetry loss as `infrastructure_abort`; forbidden assistance is `diagnostic_only`; clean insufficient facts remain `incomplete_evidence`.

Provisioning evidence is freshly reconstructable. The deterministic `validation_provisioning_verify` and `validation_scenarios` DVC stages were regenerated and pushed; route object `574a3a8ea3ddf1d6b8dddd892c4d1c18.dir` contains the exact roster identity. Fresh DB-backed evidence is remote-synchronized at `artifacts/cata_raid_program/phase1_provisioning_readback_278b3e51_20260812.dvc`, object `83b3b92c4c66f5718d65005437a9de07.dir`, 68,142 bytes/two files. Its exact BWD readback SHA-256 is `fcd2bea7e1b4b4399154526f67d2c0c28dd478a61d3b800ae3581b645d6ab78e`; the full generated/live verifier SHA-256 is `96062abfe2bdcf806d570b1d3e58148ad1f0d44a15bb5756d3babbb8cca27558`. Both pass, with exact map 669 entrance positions, offline/unleased pool state, zero group/instance residue, all 50 accounts/characters and runtime gear/rotation facts valid. The focused suite passes 227 tests; the coordinator's previously raced test passes in a full 32/32 rerun; Python compilation and DVC local/cloud status pass.

Exact next gate is a fresh independent Sol-high review of the status descendant of `a9994c4aee`. The successful `6ffcf7d9c1` binary remains intentionally stale. Only zero unresolved Critical/High findings authorize fast-forwarding the isolated build worktree, exact policy-v8 configure/build, regression/readback recheck, and uncapped canonical Phase 1 restart. Magmaw remains strictly a non-fidelity native engagement/wipe/reset/recovery smoke; no kill or 4.4.2 mechanic claim is authorized. All 464 material fidelity rows remain fail-closed.

## 2026-08-12T16:37:51Z — Atomic saved-roster and shared boss-focus remediation ready for exact review

The mandatory Sol-high review rejected exact `1affcc2265` with two High findings before any stale build or live restart was accepted. Checkpoint `cd99025d89a7ac13a6bfb5f7f9b0fe1bc535de71` now plans all ten unique BWD GUIDs and resolves every saved position against the manifest's exact map 669 entrance and bounded XY/Z identity before the first lease, native login, spawn, or group creation. Missing, cross-map, or same-map-stale placement therefore leaves zero newly claimed bots, leases, or group and cannot fall through to configured-center or race-start repair. The generated eight-row BWD route was selectively hydrated and independently confirmed to carry the same entrance identity and expected count on every row, then its exact workspace payload and two child cache objects were re-evicted while retaining DVC metadata.

The second High is also closed in source: the later shared group-focus branch on a boss route now rejects undeclared creatures and routes a declared target through the same typed `TryBossMechanics` authority. It no longer reaches unrestricted generic damage with permissive area/multidot defaults. This remains only a Phase 1 Magmaw native engagement/wipe/reset/recovery scope; it supplies no timer, summon, spell, target-rule, or 4.4.2 fidelity claim. The focused foundation/runtime/capture/coordinator/Phase 9 suite passes 218 tests and `git diff --check` is clean.

The provisioning report mismatch is closed rather than carried forward. Manifest paths are repository-relative and byte-reproducible across the integration and isolated worktrees at `95e8e398aa`; strict reconstruction and live DB/runtime checks pass, and regenerated `validation_gear` object `14bceda375f6e9ae504ed8e80a001d0f.dir` plus `validation_provisioning` object `df7c4d397b5b7c0329f44faba7eeb024.dir` were pushed and remotely verified. The third uncapped capture now has a tracked compact classification/cleanup/hash summary in addition to its remote-backed `.dvc` pointer. Its exact 15.8 MB payload and three child cache objects remain evicted. Future Phase 1 output directories are ignored by a directory-only wildcard that deliberately does not ignore their `.dvc` pointers, preventing capture-time Git identity drift.

No current binary authorizes a live restart: the successful `6ffcf7d9c1` receipt is intentionally stale. Exact next gate is the independent Sol-high review of the status descendant of `cd99025d89`; only a zero-critical/high result authorizes a policy-v8 configure/build at that exact clean commit, followed by regression/readback verification and a new uncapped serialized Phase 1 capture. Root still owns worldserver and all future diagnostic-shard setup. Live Luna-xhigh boss babysitters start only after every shard is fully provisioned, independently identity-verified, capture-partitioned, and boss-ready. All 464 material fidelity rows remain fail-closed.

## 2026-08-12T16:18:00Z — Phase 1 third uncapped capture diagnosed, published, evicted and reprovisioned

The mandatory Sol-high review approved exact `6ffcf7d9c19c9b4291d065fc687079cc5f2a75b6` with zero unresolved critical/high findings. Policy-v8 configure and one-job worldserver build then succeeded at that exact clean commit. The verified trusted-local receipt binds worldserver SHA-256 `e46f032a583aedb7e19aa3a16baf93f86ee7ef6353c68b404f3c0c4a6f344f07`; the post-build focused suite passed 211 tests. A first worldserver attempt correctly aborted before compiling because receipt files inside the worktree made source provenance dirty; both that provenance-abort receipt and the clean successful retry receipt are retained. Future receipts are written outside the worktree until the build finishes.

The third canonical capture ran uncapped and stopped cleanly as `incomplete_evidence` after 301 seconds and 60 unchanged objective samples. Ten bots and ten leases existed, but `Bwdtanka` loaded from stale persisted map 0 coordinates while the other nine entered BWD instance 1. The exact raid runtime therefore remained inactive in formation, every bot returned `validation_cohort_live_instance_pending`, no boss was engaged, no identity was frozen, and no assistance occurred. Status, diagnose, trace and raw server logs agree. Cleanup proved zero bots/leases and no worldserver/process overlap. The immutable three-file diagnostic batch is remotely verified at `artifacts/cata_raid_program/phase1_raid_foundation_6ffcf7d9_20260812.dvc`, object `3b098a87841a913ea9c8febd975f6703.dir`, 15,828,763 bytes. Its workspace payload and exact three child cache objects are evicted; only the pointer and tiny directory metadata remain.

Authoritative provisioning was reapplied through 50 account and 5,764 character statements. Independent readback proves exactly ten BWD characters, all offline, enabled, unleased, tagged `blackwing_descent_10n`, map 669 at the frozen entrance, exact 2/3/5 roles, and zero stale character-instance/group-member rows. The broader provisioning verifier reports the live DB and runtime gear/spell/talent/glyph surfaces valid, but separately flags a preexisting generated `dataset/validation_provisioning/report.json` content-hash mismatch; this is retained as a reproducibility issue and is not misclassified as DB application failure.

Exact remediation commit `1affcc2265` rejects any validation saved position whose map differs from the route map and forbids race-start/near-player fallback when validation resume placement is missing or invalid. This prevents another partial split cohort without relocating a live member or weakening native identity. The expanded focused suite passes 212 tests. Mandatory Sol-high exact review is active; only after zero critical/high findings may the isolated worktree rebuild and restart the uncapped capture. Six Luna-xhigh BWD boss preparation audits are complete, but no live boss babysitter may start until root has a worldserver and every disjoint boss shard fully provisioned, identity-verified and boss-ready.

## 2026-08-12T15:42:16Z — Phase 1 exact review checkpoint and per-boss babysitting/storage policy bound

Exact source checkpoint `dfe6018f8f836434feb55b2496efa446df1038c4` closes the live-instance and uncapped-monitor findings without claiming raid completion or 4.4.2 mechanics fidelity. The cohort now freezes only a positive exact roster map/instance/group/leader identity, resolves released-ghost corpse provenance from that frozen raid instance, rejects later group or leader rebinding, and gives Magmaw only the engagement-scoped `focus_fire` authority for encounter entry 41570. Canonical runtime remains uncapped. Its monitor advances only on monotonic objective facts and separately requires fresh status, diagnosis and trace output within 30 seconds; channel loss is a clean `infrastructure_abort`, while sustained objective non-progress preserves an `incomplete_evidence` diagnostic bundle. The expanded Phase 1/coordinator/compile-shape/Phase 9 focused suite passes 208 tests. Mandatory exact Sol-high review and a new policy-v8 build receipt are still pending.

The user-amended ignored plan is now bound at SHA-256 `c9ccf4c742d6048556d44d5eec2536cb5be2fd2c4bf7885471989356c117f843`. Each live boss diagnostic shard receives one dedicated `gpt-5.6-luna` `xhigh` babysitter whenever harness capacity permits, but root must first start the one worldserver, create and independently verify every disjoint cohort/group/instance/save/attempt/roster/strategy identity, apply the tracked prerequisites, bind capture partitions, pass process/MySQL/resource preflight, and leave every shard boss-ready. Only then are live babysitters spawned, so they begin their individual run and telemetry monitoring immediately rather than provisioning, repairing or waiting for setup. The six currently active BWD workers are explicitly read-only script/decision contract-preparation auditors, not live babysitters. A live sitter owns only that boss's status/diagnose/trace interpretation and must compare actual encounter-script behavior with bot observations and decisions; root retains worldserver, mutation, capture and publication authority. Diagnostic storage is immediate-lifecycle: hydrate only an exact needed partition; once diagnosis is independently reconstructed, its resulting fix/review/status checkpoint is secured, and targeted DVC push plus remote verification succeeds, exactly evict that workspace payload and its exact temporary cache objects. Retain compact hashes, receipts, failure fingerprints and `.dvc` pointers; never use broad `dvc gc`.

The validation-scenario stage is remotely synchronized at object `b16a253cfa567b9cfcc04f8307452d6e.dir`. The prior Phase 0 and two Phase 1 failed-live payloads remain exactly evicted. The isolated build worktree is clean at stale build checkpoint `4012450bb9`; no worldserver, build, compiler, capture writer or publisher is active, and MySQL is healthy. Exact next action is to complete the Sol-high review of `dfe6018f`; if it reports zero unresolved critical/high findings, fast-forward the build worktree, run the receipt-bearing policy-v8 configure and one-job worldserver build, rerun the 208-test focused gate, then begin the uncapped serialized BWD 10N Phase 1 capture. All 464 material fidelity blockers remain fail-closed.

## 2026-08-12T15:21:18Z — Phase 1 pre-build review findings remediated without adding a raid time cap

The mandatory independent review of the initial live-instance patch found two additional High blockers before any rebuild. First, an outside released ghost cannot safely use `Player::GetCorpse()` because that lookup queries its current graveyard map; the exact corpse is now resolved and null-checked from the frozen raid map/instance and verified against owner/map/instance. Second, all checked-in BWD boss rows lacked typed mechanic contracts, so the fail-closed runtime would suppress combat forever while an uncapped capture had no semantic-stall terminal.

Exact implementation checkpoint `1109e663d9f6e78077e85dd4bfa9a8b317e32d4f` closes both. Magmaw now carries only the typed identity `phase1_magmaw_native_engagement_recovery_v1`; empty mechanic fields deliberately assert no timers, values, radii, add counts or fidelity, but permit native combat needed for the Phase 1 wipe/reset/recovery smoke. Canonical capture remains raid-duration uncapped and polls status/diagnose/trace every five seconds. It now hashes only semantic progress—route/boss/native generations, boss states, health/target/decision facts and relevant metrics—and stops as `incomplete_evidence` after 300 seconds and 12 samples with no semantic change, preserving the diagnostic bundle for fix/review/rebuild/restart.

The validation scenario stage was regenerated through Pixi/DVC and pushed. Object `081bb1901c16611664791d71235f2327.dir` is remotely in sync; the Magmaw source remains 41570 and its DB-grounded navigation anchor remains Room Stalker 47196/GUID 250087. The focused capture/runtime/generator suite passes 55 tests. Mandatory exact-commit Sol-high re-review is pending; do not build or rerun before zero unresolved critical/high findings.

## 2026-08-12T15:10:47Z — Phase 1 second live failure published; raid validation is now uncapped and instance identity is remediated

Exact policy-v8 configure/build at clean `4012450bb9316c4b7dd739aefe58856c9cdc1b3b` succeeded through the queued coordinator. The trusted-local configure and worldserver receipts are tracked under `experiments/configs/cata_raid_build_receipts/`; the receipt-bound worldserver SHA-256 is `45dca82d24b4e800dfa241be2c4fcb21dfb70b43ae5317c3205c621c92dcf55b`. MySQL and process isolation remained healthy, the build saw no resource-pressure reason, and the exact clean worktree identity stayed stable.

The second canonical BWD 10N run completed its then-configured 900-second diagnostic window with zero accepted stable statuses. It never engaged a boss. Status, diagnose, trace, and the raw server log independently reconstructed the infrastructure defect: the first saved-position leader was relocated to map 0 during native login; formation then combined configured map 669 with instance 0 and froze that synthetic identity before the remaining nine bots entered legitimate BWD instance 2. Runtime consequently retained instance/lockout 0, map difficulty -1, no boss-state observer, and correctly fail-closed every bot as outside the immutable cohort. Cleanup, build provenance, source identity, process exclusion, and forbidden-assistance checks passed. This is not gameplay failure and supplies no native recovery acceptance evidence.

The immutable three-file failure batch is remotely verified at `artifacts/cata_raid_program/phase1_raid_foundation_4012450b_20260812.dvc`, object `3026ddaf3954d42646e780ce84df3a44.dir`, covering 103,384,402 bytes. Its exact workspace payload and four exact cache objects were evicted after remote verification; the pointer remains recoverable. No broad DVC garbage collection was used.

Commit `544f774d5a0c3897f2144310f9469f46fb787266` remediates the defect without weakening identity. Validation passes its already-resolved saved placement through native login; partial rosters may form the native group but cannot freeze identity or emit formation acceptance; the exact roster must be in one positive live map/instance before identity locks; pending formation blocks route decisions; a frozen identity can never rebind; and released-ghost exemption now requires the corpse's exact frozen map and instance. Canonical capture is also terminal-gate driven by default: `--observe-sec 0` is uncapped, so raid duration alone never ends a healthy run. Status, diagnose, and trace continue every five seconds. A sustained semantic stall must be diagnosed against both the encounter script and each bot's observed decision, then stopped cleanly, fixed, independently reviewed, rebuilt, and restarted.

The user-approved plan amendment is bound at SHA-256 `10ed060d96b51dc3bacc17d582530de1e9c832e8545e415c583e2e0ed16afab2`. It adds one diagnostic cohort/native instance/save/evidence partition per boss on one worldserver (six for BWD), dependency-DAG-derived diagnostic predecessor states, and three sequential full-clear qualification runs with a fourth on material disagreement. Shards are diagnostic-only. The BWD Nefarian shard must use its own predecessor-complete diagnostic save, form and prepare on the upper ledge, and prove the legitimate jump/descent before native engagement below; floor-spawned or descent-skipping attempts are invalid.

Patch-focused validation passes 54 tests. The broader diagnostic has no new failure relative to exact `4012450`: the patched worktree reports 329 passed/34 inherited stale shape failures, while the untouched control reports 326 passed/35 failures. Exact next action is the mandatory independent Sol-high review of `544f774d5a`, explicitly auditing encounter-script behavior against bot decisions. Only after zero unresolved critical/high findings may the isolated build worktree fast-forward, rebuild through policy v8, and start the uncapped canonical rerun. Phase 1 and Phase 2 remain unaccepted; all 28 research packets and 464 material fidelity blockers remain fail-closed.

## 2026-08-12T14:36:49Z — Phase 1 canonical capture isolated and remediated three infrastructure defects

Exact policy-v8 configure/build at clean `7f4833e6f78c7a1cdc7747a96b6e4cad4f7509b5` succeeded through the queued coordinator. The receipt-bound worldserver SHA-256 is `5dc3d91c316451abfb9dfa36fc8442154561ece3d7d50ad1a3a586d916d8ad97`; MySQL stayed healthy, minimum available memory was about 17.95 GiB, memory PSI remained zero, and the post-build focused suite passed 200 tests. The mandatory independent `gpt-5.6-sol` high review found zero critical, high, or medium issues. Exact receipts are retained under `experiments/configs/cata_raid_build_receipts/`.

The serialized 900-second BWD 10N Phase 1 capture then proved the exact 10-member roster, 10N difficulty, instance/lockout identity, build/profile binding, process exclusion, watchdog, and zero-bot/zero-lease cleanup, but did not enter Magmaw. It deterministically stopped at route node 1 with `route_destination_invalid_z_transition`; a legitimate temporary weapon enchant also changed the supposed permanent roster identity, and the assistance scanner inverted `blocked_no_fallback` into a positive fallback marker. This is classified as failed live infrastructure, not gameplay failure. The immutable failure batch is remotely verified at `artifacts/cata_raid_program/phase1_raid_foundation_7f4833e6_20260812.dvc` (`b6ad9092e9716e3535f0b1f7766a49e9.dir`) and its exact local payload/cache were evicted.

Commit `d6262156c8eccb1bb4f0f9f272e96e13fef7c430` remediates all three findings: Magmaw keeps creature 41570 as the encounter source but navigates to DB-sourced Room Stalker 47196/guid 250087 at `-307.531,-35.4375,211.815`; permanent gear identity zeroes only `TEMP_ENCHANTMENT_SLOT`; and `blocked_no_fallback` is explicitly negative evidence while actual fallback markers remain forbidden. The validation scenario DVC stage is remotely in sync at `5860365b5c53a066409b96ceaeded02f.dir`; 171 focused tests pass.

Exact next action: fast-forward the isolated build worktree to the newest status descendant of `d6262156c8`, run the receipt-bearing incremental worldserver build through `pixi run raid-build`, independently verify it and the focused regressions, then rerun the serialized Phase 1 capture and publish/verify/exactly evict the accepted batch. Do not claim Phase 1 until native engagement, full wipe, boss reset, per-GUID release/runback/re-entry/resurrection, identity-scoped ready check, demultiplexing, forbidden-assistance, cleanup, and DVC gates all pass. Continue automatically to Phase 2 only after that gate.

## 2026-08-12T13:18:58Z — Local build trust authorized; Phase 1 configure/build approved

The user explicitly authorized production build receipts created and verified by local operator `runiir` (UID/eUID 1000). This authorization is limited to build-receipt authenticity: gameplay, 4.4.2 fidelity, canonical live evidence, independent review, cleanup, DVC publication, and every other raid gate remain unchanged and fail closed. Policy v8 records the exact local trust model and all existing source, cache, generated graph, toolchain, environment, command, queue, and output bindings. The external Ed25519 signer/ledger protocol remains available as optional hardening but is no longer a Phase 1 prerequisite.

The mandatory independent `gpt-5.6-sol` high review of exact clean commit `3aa35b1d6a64789a2fec5c59cebbabe3cb872d87` found zero critical/high issues and approved the trusted-local receipt gate and coordinated configure/build. Its two medium transparency issues are closed in the next checkpoint: failed/nonzero receipts cannot report `gate_bearing=true`, and the tracked service documentation/status no longer misstates the optional external signer as blocking. No heavyweight job, worldserver, operator, capture writer, or DVC publisher is active; MySQL is healthy.

Exact next action: commit this coherent trust/status clarification, fast-forward the isolated Phase 1 build worktree to that descendant, execute the exact policy-v8 Release configure and one-job worldserver build through `pixi run raid-build`, independently verify both receipts and regressions, then run the serialized BWD 10N native formation/difficulty/wipe/reset/recovery capture. If accepted, publish and remotely verify its DVC batch, exactly evict it, close Phase 1, and continue to Phase 2. All 28 research packets remain complete but fidelity-blocked by 464 unresolved material ledger rows; no value may be guessed or tuned around bot capability.

## 2026-08-12T13:02:36Z — Privileged build protocol reviewed and ready for external provisioning

The exact reviewed protocol checkpoint is `a0dbafca4dd3bfce1c1670e45b6f804a57f3b028`. The mandatory independent Sol-high review reports no critical, high, or material medium findings and explicitly sets `protocol_ready_for_external_provisioning=true`. Production verification is pinned to the tracked policy-v8 path and service-config SHA-256; canonical capture has no policy or service override. The build signer and ledger signer must have distinct key IDs and public-key hashes. The verifier reconstructs the complete local coordinator receipt, checks Ed25519 build attestation, then fetches a fresh policy-pinned HTTPS ledger assertion proving unique, included, non-revoked binding of sequence/head, record ID, attestation-record hash, payload hash, and receipt hash. Test relaxations require an actual coordinator test receipt and always report `gate_bearing=false`. The focused review suite passed 207 tests; the integration owner's broader focused suite passed 196 tests.

The tracked service contract remains deliberately `unprovisioned_external_authority_required`. Exact external inputs now required are: reviewed service/key IDs; two distinct Ed25519 public keys and hashes; submission and ledger HTTPS endpoints; and a separately privileged execution/ledger principal inaccessible to the capture UID. Provisioning changes the tracked service config and policy pins, so that descendant must be reviewed before the service runs the exact policy-v8 configure/build. No heavyweight build or canonical live capture may start before this external state exists and verifies.

## 2026-08-12T12:38:10Z — Phase 1 blocked on separately privileged receipt authority

Current integration HEAD is `42357ba662c2add889bf697c6719a901ad822df2`. The mandatory independent `gpt-5.6-sol` high review reports zero locally closable critical/high findings. Policy v8 now enforces one compiler and linker, Release `-O1 -DNDEBUG`, no Unity/PCH/debug/IPO/launcher overrides, the exact hashed CMake/Unix Makefiles configure and worldserver build commands, full cache and generated Make graph stability, declared compiler/tool identities, positive environment allowlisting, configure-to-build lineage, source identity, and ticket/output bindings. The focused coordinator/capture suite passes 52 tests; the broader focused Phase 1/Phase 9 suite last passed 193 tests. No heavyweight job, worldserver, operator, or DVC publisher is active; MySQL is healthy and targeted DVC state is up to date.

The remaining build-provenance blocker requires new external authority. A separately privileged UID/service, remote signer, or hardware-backed signer must exclusively own the private key and append-only ledger and execute/sign the admitted configure/build. The capture principal must be unable to mutate that state or read the signing key. This user-level session cannot safely create or claim that boundary; repository-local hashes, duplicated ledgers, or same-user keys do not satisfy the mandatory reviewer. Therefore no gate-bearing configure/build or canonical live capture was started at this checkpoint.

Exact resume condition: provision the privileged signing/execution service and provide its submission and public-verification interface. Then run the exact policy-v8 configure and worldserver build through it from clean `42357ba662c2add889bf697c6719a901ad822df2` (or a reviewed descendant containing only the service integration), independently verify the signed receipt, and continue the serialized BWD 10N formation/difficulty/wipe/reset capture and DVC publish/verify/exact-evict gate. All 28 boss research packets remain complete but fidelity-blocked by 464 unresolved material ledger rows; Phase 2 must not begin by guessing values.

## 2026-08-12T06:51:24Z — Phase 1 9a7 rejection remediated at 35c9; Release build retry next

The mandatory Sol-high review of exact checkpoint `9a7b3c72aed5a2e464635b31e8f4617f2134c8d8` rejected Phase 1 with one critical, six high, and five medium findings. Checkpoint `35c9e61b85407d2ccbe6f3be1b3eaae8ce6eb407` remediates the critical/high findings: terminal cleanup identity is preserved; recovery reset and per-member evidence ordering are independently bounded; profile/assignment/leader identity cannot drift; ready checks are assignment-scoped; receipt-v2 source provenance and all-row demultiplexing fail closed; the live mechanic contract rejects unknown/incomplete fields and executes the required typed generic primitives; kill synchronization no longer has an unreachable hold predicate; and the inherited Phase 5 static-contract regression plus coordinator pressure-test race are fixed. The focused Phase 1, Phase 9, coordinator, model, and targeted legacy regression suite passes `168 passed`; this is implementation evidence, not gate acceptance.

The policy-v5 no-unity and policy-v6 no-PCH one-compiler builds both ended as `build_resource_abort` under the unchanged memory-PSI ceiling; MySQL remained healthy and source provenance stayed clean. Policy v7 preserves every resource threshold, one compiler, one linker, no unity, no PCH, and low-memory linking, while switching from `RelWithDebInfo` to `Release` to reduce debug-information memory. No heavyweight job is active. Exact next action: commit this status checkpoint, fast-forward the isolated Phase 1 build worktree, run a clean coordinated v7 Release configure/build, then request a fresh independent Sol-high review of the built commit. Canonical live capture remains forbidden until both pass.

All 28 boss research packets remain complete and fail closed with 464 unresolved material values; BWD has 52. Phase 2 fidelity remains blocked and no value may be guessed or tuned around bot capability.

## 2026-08-12T05:14:39Z — Phase 1 remains open after prior Sol rejection; policy-v5 no-unity build active

Current integration HEAD is `e07638a31ef5e832269065451cfa36da704affcb`; the latest Phase 1 foundation/remediation checkpoint is `a12299aa1d6e02eb50902ef536c1fcc71487fae0`, descending from the frozen local `master` raid base. The earlier mandatory `gpt-5.6-sol` high adversarial review rejected the pre-remediation foundation. Its critical/high remediation is now present, but this is not an acceptance: the mandatory fresh review, a successful current build receipt, and the live DVC gate remain pending. No Phase 1 pass is claimed.

The remediated foundation owns cohort-scoped raid identity, exact 10/25 size and 10N/25N/10H/25H difficulty readback, map/instance/lockout identity, server epoch/attempt, frozen roster slots, deterministic five-player subgroups, unique leases, assignment/evidence identity, native boss-state observation, wipe/reset/recovery counters, and fail-closed capture reconstruction. Generic formation, target, interrupt/dispel, healer, cooldown, soak, object, vehicle, transport, platform, altitude, cross-platform, recovery, and capture-demultiplex primitives are implemented; unknown or incomplete fields remain rejected. Implementation is not live acceptance evidence.

All 28 required boss research packets are now complete: BWD 6, Bastion 5, Throne of the Four Winds 2, Firelands 7, and Dragon Soul 8. The packets contain 464 unresolved ledger blockers in total; all 28 remain `fidelity_blocked`, and zero bosses are promoted to 4.4.2 fidelity. The BWD quantitative audit records 52 blockers: six cutoff-identity resolutions, three partial client-row resolutions, six repository-baseline resolutions, and 37 items still requiring exact 4.4.2 logs or authoritative hotfix evidence. The official global Dragon Soul cutoff remains `2025-02-20T23:00:00Z`; build 59185 remains frozen. The bounded primary client spell extract is pinned at DVC object `eff38325fc3aeac0fa15d0c81b2be901`, pointer SHA-256 `ceb785fb8e1e902ed3de43b6a9ecf8c22502c9d8d2737f7156b95407293f7234`, and payload SHA-256 `481c903afd07a03b8e705fa2c776106f71f5a136d38ece97dfce35b1ead002d3`; its exact output was evicted after verification.

Build history is preserved through the policy-v5 retry. The three-compiler, two-compiler, and earlier one-compiler attempts retain their `build_resource_abort` or prior-success receipts; the current `cata_raid_build_resource_policy_degraded_v5.json` run has one compiler, one linker, Unity disabled, and no result or receipt yet. It is the sole heavyweight job and must finish before any live validation. Resource pressure remains classified as `build_resource_abort`, never gameplay failure.

Next gate order is strict: complete and verify the active policy-v5 build, run the mandatory fresh Sol high review with zero unresolved critical/high findings and explicit fidelity approval, then execute the serialized BWD 10N native formation/difficulty/wipe/reset/recovery capture, independently reconstruct acceptance, publish and remotely verify DVC evidence, and exactly evict the verified payload. Phase 2 remains blocked by the unresolved BWD quantitative and exact-log/hotfix evidence.

## 2026-08-12T04:31:00Z — Phase 1 implementation checkpoint (superseded; acceptance not granted)

This historical checkpoint overstated completion. The prior Sol high review rejected the pre-remediation foundation; see the newer entry above. The runtime implementation and remediation are retained, but the mandatory re-review, current policy-v5 receipt, and serialized live DVC gate remain open.

The Phase 1 foundation now owns exact cohort-scoped raid group/leader, 10/25 size, canonical 10N/25N/10H/25H difficulty write/readback, map/instance/lockout, server epoch/attempt, roster slots, deterministic five-player subgroups, unique leases, assignment generation, strategy, and evidence identity. Status, diagnose, trace, and event context expose the same runtime. Native instance observation now records all boss states, encounter-in-progress, alive membership, wipe generation, boss-reset generation, recovery generation, and reconstructed ready state instead of inert placeholders.

The declarative synthetic compiler covers stack, pair, lane, quadrant, ring, spread, cone, behind/front exclusion, raid/role/subgroup anchors, all required tank-swap triggers and target controls, interrupt/dispel backups, healer/cooldown/soak/immunity/personal/battle-resurrection policies, objects, extra actions, vehicles, transports, jump pads, platform/altitude/flying/cross-platform movement, recovery, and capture demultiplexing. Unknown or incomplete fields fail closed. The focused research/runtime/capture suite passes 138 tests; the runtime/foundation/capture subset passes 128.

The first three-compiler build exposed and fixed an inherited incomplete `Pet` include, then ended as `build_resource_abort` from swap growth. The two-compiler retry reached 74% before the coordinator observed sustained memory PSI while linking `libgame.a`; it also ended as `build_resource_abort`, with about 18.8 GiB minimum available memory and healthy MySQL. Both receipts are preserved. Policy v3 retains every reserve/PSI/load/disk threshold while reducing the retry to exactly one compiler and one linker. A clean reconfigure at commit `2db8059867f15e58721b92da01f8b7ccf26346cf` passed with a valid receipt; the coordinated one-compiler worldserver build is active and no live validation may overlap it.

Research now covers all required encounters through Hagara, Ultraxion, and Yor'sahj. Warmaster Blackhorn, Spine, and Madness are the final active exact Luna xhigh packets. The global research identity was corrected from the patch-note publication timestamp—which predates raid availability—to Blizzard's official Dragon Soul global unlock, `2025-02-20T23:00:00Z`. Build 59185 is unchanged. Later hotfixes and the March Presence modifier remain excluded from the primary snapshot. Material values remain fail-closed pending primary client data and known-mode exact-cutoff evidence.

Exact next action: let the coordinated one-compiler build finish, retain/verify its receipt, run the mandatory Sol high adversarial foundation review after a research slot frees, integrate any critical/high corrections, and only then run the canonical serialized BWD 10N formation/wipe/reset/recovery capture from a clean reviewed commit.

## 2026-08-12T03:49:30Z — Phase 0 accepted; Phase 1 opened

Phase 0 is accepted at foundation checkpoint `72323f589090bf9d181da986cd277b1377f8bbaf`, descending from frozen `raid_base_sha` `889d38cc9451c2b8104db142ce069593b4647a41`. The clean-worktree no-bots baseline ran at commit `c5654f0facc06cacb0e568c08d5002ce15813a78` with worldserver SHA-256 `e9a5878734a248fe5694d92297d0dacd06abc73ec0efe950111544ff5ed27ed6` and config SHA-256 `e4059209ab34173259000f58b57c405361b3e97df21d20162687e08d73f727d7`.

The passing 30-second measurement observed zero bots and zero leases, clean worldserver exit, stable clean Git identity, 30 resource samples, maximum RSS 1,439,461,376 bytes, mean CPU 1.401% of one core, maximum 1-minute load 0.383, minimum `MemAvailable` 21,844,103,168 bytes, zero memory PSI, and six successful MySQL probes with maximum latency 43.829 ms. The compact report is remotely verified at `artifacts/cata_raid_program/phase0_no_bots_worldserver_20260812.dvc`, object `fa6ca050a2ddd41eece7f05d89424a2f.dir`. After remote verification, only that exact output and its two exact cache objects were removed; the pointer can recover it. The first parser-only attempt was classified `infrastructure_abort`, not gameplay failure, because the harness incorrectly expected an `active_bots` field instead of the runtime's `bots` plus `lease_count`; the parser was corrected and tested before the successful fresh identity.

Phase 1 is active. Its first task is a fail-closed audit of the existing one-cohort runtime against raid GUID/size/difficulty/lockout ownership, five deterministic subgroups, slot leases, assignments, generic formations/swaps/targeting/interrupts/dispels/healer/soak/interactable/platform/recovery primitives, difficulty readback, and immutable evidence demultiplexing. Do not assume prior BWD experimental artifacts certify any raid gate; the corrected BWD route remains blocked until native unlock behavior is proven.

## 2026-08-12T03:41:47Z — Phase 0 policy, target, readiness and route audit checkpoint

The launch target is now frozen as Cataclysm Classic `4.4.2.59185`, locale `enUS`, with the hotfix cutoff at the official Blizzard version-1 launch-note timestamp `2025-02-18T19:31:51.916Z`. The primary launch-note topic/post/version metadata is pinned, as are pre-cutoff BigWigs Cataclysm tag `v11.0.13` commit `650bab03981eb06b5fa6ded88e47c523caa3c7c3` and DBM Cataclysm commit `4b02efec4552aef3df43c75fb19c6d8c7fdb3e6e`, including archive SHA-256 identities. Primary client-data hashes, a pinned Wowhead 4.4.2 quantitative extract, and known-mode logs remain unavailable, so all unresolved material BWD rows remain fail-closed and no boss is labeled verified.

The raid-wide source/loader/read-only DB audit records 28 catalog endpoints: 19 dedicated sources present but not runtime-validated, nine missing boss implementations (Sinestra, Beth'tilac, and seven Dragon Soul bosses), plus one separately incomplete Dragon Soul instance foundation. Instance templates, map spawn/gameobject counts, entrance teleports, BWD direct spawns and native summon groups were checked without DB writes. Existing source or DB shape never promotes runtime readiness.

The BWD 10N route was independently reconstructed. Its prior Atramedes instance-data injection and Nefarian instance-data injection/direct spawn were forbidden certification assistance and are removed. The generated route now contains zero nonzero boss activation-data IDs and zero boss activation-summon entries. Its 2/3/5 provisioning was also corrected from excluded Protection Warrior to permanent-roster Protection Paladin. `validation_gear`, `validation_provisioning`, and `validation_scenarios` were regenerated through Pixi/DVC and pushed; cache and remote are in sync. The scenario remains explicitly blocked until native Atramedes/Nefarian unlock, start, reset, difficulty, and credit behavior is observed.

The focused Phase 0 policy, coordinator, no-bots harness, research, and inherited qualification suite passes `23 passed` (the latest policy subset passes `16 passed`). The current full legacy diagnostic reports `289 passed, 23` inherited stale structural/watchdog/provisioning failures; the first scenario-builder failure is the handed-off Stonecore expectation for an absent `Ozruk approach clearance` node. Stonecore Phases 10–12 were not resumed. The clean production worldserver receipt verifies as canonical receipt SHA-256 `480bb668f3cd4081a334559b35dd09abe1ea69e80305888df9645de9b250efad` at commit `8eb54f0160b3c3d986ed944f9d64ebf83922c0f8`.

The user-requested all-boss research queue is running at the harness maximum: one exact `gpt-5.6-luna` xhigh turn per boss, three simultaneous workers, immediately refilled. BWD six were already complete; Halfus, Valiona/Theralion, Ascendant Council, Cho'gall, Sinestra, and Conclave are complete, with later T11/T12 bosses in progress. Every package remains fail-closed where quantitative evidence is unresolved.

Exact next action: commit this policy/readiness/route checkpoint, confirm no live identity-bearing process, then run `pixi run raid-no-bots-baseline` once from the clean commit using the already coordinator-built worldserver, publish the compact baseline through DVC, and close Phase 0 only if its resource/MySQL/zero-bot/identity checks pass.

## 2026-08-12T02:42:30Z — First production coordinator jobs passed

Committed coordinator foundation `ae07ef2aa233554c147f476fa456da6cfd78912a` admitted two real production jobs. Queue sequence 1 reconfigured the existing Unix Makefiles build tree with the coordinator's three-compiler/one-linker controls and produced verified receipt SHA-256 `50f58b5fc43b4b3e3731543a6ccfc13120583923818569d9605b989e4a89d257`. Queue sequence 2 built `worldserver` successfully and produced verified receipt SHA-256 `a2504d232815dfd30681841214fb056d8bbc0ba7c009a21735839a2f6aa030cc`. The resulting binary SHA-256 is `fb444fb63b88318cf63837012b64a32934caed6c0563293feff5daa18a5a0c7a`.

The build used at most three compiler jobs and one linker, recorded zero pressure reason, peaked at load 0.61, retained at least 23.3 GB `MemAvailable`, and left MySQL healthy with no coordinator/compiler/linker/worldserver process. Both canonical receipt hashes independently verify. The configure request began clean; the build request correctly records dirty state only because the first receipt was then untracked. Checkpoint these receipts and status, then rerun the already-current worldserver target once to obtain the final production receipt from a clean request identity.

## 2026-08-12T02:40:30Z — Build coordinator foundation ready for real job

The host-wide build coordinator and frozen resource policy are implemented in `tools/raid_program/queued_build.py` and `experiments/configs/cata_raid_build_resource_policy_v1.json`, exposed through `pixi run raid-build`. CMake consumes the coordinator lease identity, caps compilation at three jobs, creates one-linker job pools, and serializes link rule launch through the Git-common lock. Queue state and sanitized receipts live beneath the shared Git common directory, so all worktrees use one FIFO lease.

Eight adversarial tests pass: fan-out validation and shell-wrapper rejection; deterministic FIFO admission; queued-waiter cancellation; PID/start-time-safe stale recovery; actual killed CLI owner recovery; synthetic sustained-pressure abort with complete descendant process-group cleanup; two simultaneous CLI runs with non-overlapping admissions and canonical receipts; and common queue identity across every registered worktree. Production receipt verification rejects synthetic/test receipts. The combined coordinator, BWD research, and inherited Phase 9 qualification suite passes `15 passed`; `git diff --check` is clean. MySQL remained healthy and no worldserver/operator/publisher or compiler/linker process was active.

The coordinator has not yet claimed its real integration-build gate. Commit this foundation first, then invoke the first clean CMake configure/build only through `pixi run raid-build run`, retain and verify its production receipt, confirm zero orphan compilers/linkers and MySQL responsiveness, and update the status before proceeding to boss worktrees.

## 2026-08-11T19:34:04Z — Six-boss BWD research wave completed

Six boss-scoped `gpt-5.6-luna` xhigh turns independently revised Magmaw, Omnotron Defense System, Maloriak, Atramedes, Chimaeron, and Nefarian. Each turn was restricted to its human dossier, machine contract, and value/timer ledger; agents did not delegate, build, run live validation, mutate DB/DVC, or commit. Because the harness has three worker slots, the same three idle agent threads were reused for the second three boss-scoped turns, with fresh disjoint prompts and no inherited acceptance conclusion.

The integration owner reviewed and normalized all six packages with common contract/ledger identity envelopes, created `docs/bot_raids/README.md` and `experiments/configs/cata_raid_strategy_catalog_v1.json`, verified all 12 JSON documents, verified 25 referenced repository paths exist, and added `tests/test_cata_raid_research_contracts.py`. Focused validation passes `4 passed`; `git diff --check` is clean.

The research result is fail-closed, not a fidelity pass. The official Blizzard patch notes establish Cataclysm Classic 4.4.2 / Hour of Twilight on 2025-02-18. A public client-build table identifies 59185 as the live launch candidate, but no primary client-data extract/content hashes or exact Blizzard hotfix cutoff is pinned yet. Every boss retains material unresolved rows: timers/ranges, spell coefficients, target counts, four-mode scaling, and guide-versus-repository conflicts. The shared catalog therefore records `research_unresolved`, and all six bosses remain `fidelity_blocked`; none may be labeled `blizzlike_4_4_2_verified` or used for fixed disputed bot scheduling.

Important conflicts include Magmaw hooks/armor/parasite and heroic breath semantics; Omnotron energy/shield/rotation and heroic controller cadence; Maloriak vial/release/timer/target-count behavior; Atramedes ground-air durations, bomb counts, target rules, and Fiend cadence; Chimaeron Caustic Slime target mapping, outage probability/timing, and reset fields; and Nefarian Electrocute charge, landing/platform cadence, Dominion counts, and Shadowblaze timing.

Exact next objective: complete the queued-build coordinator tests without a heavyweight build, then pin a primary 4.4.2 client-data identity plus a 4.4.2-compatible BigWigs/DBM tag/log set to resolve or formally block each material BWD quantitative row before the Phase 0 gate.

## 2026-08-11T18:51:55Z — Phase 0 opened

The approved Cataclysm raid program is active at Phase 0. The frozen `raid_base_sha` is `889d38cc9451c2b8104db142ce069593b4647a41`, and the active integration branch is `raid/cata-raid-progression-integration`, created directly from that exact local `master` commit. Do not recreate it from `origin/master` or `all-spec-stonecore-execution`. Local `master` remains 430 commits ahead of `origin/master`; remote publication is not authorized.

The approved plan SHA-256 is `ea4cd23208fd3c3c1f2ccc533211fcd35030fb0841c35e0984bba6f03aa0e0fa`. Its ignored path is `.codex/plans/cata_raid_progression_plan.md`. The Stonecore successor evidence is inherited only as dungeon/runtime and 24-mode class-interaction evidence. The authoritative targeted-canary DVC pointer is `artifacts/all_spec_program/phase9_targeted_canary_completion_20260811.dvc`, object `38d4e71d1a4374f69fc9bf886b4df817.dir`; cache and remote were confirmed synchronized at Phase 0 start.

Initial safety checks found a clean Git worktree, no worldserver, validation operator, DVC publisher, build coordinator, compiler, or linker process, and a healthy `trinity-cata-db` MariaDB container. The host exposed 12 logical CPUs, about 30 GiB physical memory with 22 GiB available, 302 MiB swap in use, zero current memory pressure, and 46 GiB free on the repository filesystem. Broad DVC status contains inherited dependency drift and intentionally evicted outputs. The historical unavailable objects `24b9d133fe3fe1b6f253e500c266562f.dir` and `c67ee8935753b8ceccbdddbafdfebe.dir` are not new raid failures.

The stable raid contract is 25 slots: 2 tanks, 6 healers, and 17 DPS, including 12 ranged and 5 melee DPS. It preserves 24 supported modes. Feral tank is a declared offspec of the Feral DPS roster slot, not a permanent third tank; Protection Warrior remains excluded and non-blocking.

Phase 0 is not yet accepted. Exact 4.4.2 build/hotfix identities remain deliberately unresolved pending sourced research. The next coherent checkpoint is the host-wide queued-build coordinator plus frozen resource policy and adversarial queue tests, followed by the consolidated BWD dossiers/value ledgers and full raid script/DB/loader readiness audit. No boss compilation or canonical live validation may begin before the coordinator gate passes.

Exact next command-safe objective: implement `tools/raid_program/queued_build.py` and its focused Pixi tests with one heavyweight FIFO lease, three compiler jobs, one linker, an 8 GiB-or-30% memory reserve, pressure abort classification, PID-start-time stale recovery, process-group cleanup, sanitized receipts, and live-validation exclusion.
