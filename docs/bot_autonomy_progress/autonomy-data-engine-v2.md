# Autonomy Data Engine v2

## Phase 1: runtime truth repair

Status: implementation complete; live Stonecore gate not yet passed.

Changed:

- Trash liveness is independent from target selectability, evade state, and path availability.
- Trash nodes require observed engagement and verified cluster clearance; `expected_alive_count` is descriptive only.
- Boss nodes require a naturally dead target. Validation force damage, forced death, and teacher completion were removed.
- Route terminals and boss kills are scoped by exact node and generation in runtime traces and report aggregation.
- Stuck recovery requires progress after the latest stuck or target-loss event.
- Full-clear and segment reports reject unscoped counters, stale nodes, forced assistance, missing terminals, and missing per-boss kills.
- Pytest ignores `generated/orchestrator_worktrees`.

Worker routing:

- Large/high-risk diagnosis used `gpt-5.6-sol` with high reasoning.
- Medium implementation and debugging used `gpt-5.6-terra` with medium reasoning.
- Structured SQL/manifest audits used `gpt-5.6-luna` with low reasoning.
- Simple disk/DVC and predicate audits used `gpt-5.3-codex-spark` with low reasoning.
- These were direct worker Codex sessions selected from the orchestration skill; `tools/bot_ml/orchestrator_daemon.py` was never invoked. A later Sol audit launch was blocked by account quota, so the existing collaboration reviewer performed that read-only audit.

Strict live iterations:

- Runs 001-003 isolated motion resets, cohort routing, and overlapping trash scopes.
- Runs 004-005 established exact post-stuck progress and let dungeon cohorts join the tank's authoritative focus.
- Run 006 exposed the three-second combat cadence; the active route/combat cadence is now capped at one second.
- Run 007 reached Slabhide after a real Corborus kill, but its final trace was incomplete and the airborne approach failed strict Z validation.
- Run 008 verified full trace capture and terrain-projected approaches, then wiped at Corborus with the healer dying first at 17.4% boss health.
- Run 009 proved targeted aura movement for Crystal Barrage spell 86881, but also misclassified shared Dampening Wave spell 82415 and caused a manifest-route death loop.
- Run 010 was aborted before Corborus after a DBC audit proved its periodic-trigger refinement would reject 86881. The aborted diagnostic is also checkpointed in DVC.
- The current implementation matches the loaded spell shape: a harmful `PERSISTENT_AREA_AURA` with `PERIODIC_DAMAGE`, and moves away from the aura's dynamic-object owner rather than the boss caster. This accepts 86881, rejects 82415, and remains encounter-ID independent.
- Run 011 verified 20 real 86881 movement events and zero 82415 movement events. The healer was never selected by 86881 but still died first immediately after the boss submerge/add phase, followed by a death-loop watchdog stop.
- The manifest's `adds` tactic now uses a dedicated `add_target_entries` list inside the strict route handler: non-healers select only engaged listed adds with their DB-backed class/spec action profile, while the healer continues triage. Boss legality and kill evidence are unchanged.
- Run 012 proved the dedicated manifest list selects only Rock Borer 43917, never charge hazard 43743 or unrelated creatures. The healer survived the previous failure point, but two of the first three add decisions returned `no_action`; DPS1266 and the tank died next.
- Add engagement now initiates the normal auto-attack/pull alongside the class/spec action, matching the established boss-target path without changing hard masks or terminal evidence.
- Run 013 made every listed-add action succeed, but tank and DPS selected three different borer GUIDs and still wiped. A generation-scoped shared add focus now keeps the party on the first valid listed add until it dies, without touching boss focus or terminal evidence.
- Run 014 verified the listed-entry mask, successful add actions, and coordinated sequential focus: 73 complete-trace add actions covered 29 GUIDs in 29 non-interleaved contiguous blocks, with explicit dead-target evidence on several transitions. The first truthful failure is now add-wave survivability/throughput, not focus divergence. The party stopped on `validation_route_death_loop` with 21 trash kills, 11 deaths, Corborus at a 48.36% minimum, no boss kill, and exact terminals only for generations 1-4. The run contains no forbidden assistance. Its artifact is checkpointed and pushed as `stonecore_strict_repair_run_014.dvc`.
- Run 015 persistently verified 39 real Rock Borer deaths and coherent focus transitions, then exposed the actual overlap: Crystalspawn Giant 42810 at `(1150.24, 929.81)` remained alive beside Corborus because the generation-4 antechamber manifest omitted its entry. Its repeated Quake 81008 mechanics overlapped the boss/add phase before the death-loop stop. The antechamber cluster now includes 42810, so its observed clearance is required before the boss node.
- Run 016 proved that manifest membership alone was insufficient: generation 4 included 42810 but terminaled after 21 other trash kills without a 42810 kill; the same live Giant then emitted Quake mechanics during Corborus. The terminal scan had been allowed before a bot reached the node anchor, so an unloaded nearby grid looked empty. Kill-triggered clearance is now deferred and scan-based terminal requires arrival within the node radius before verifying no live pack member.
- Run 017 was intentionally aborted after the deferred-terminal path repeatedly emitted the same dead GUID 14 from the tank's stale victim, reaching 140 duplicate `mob_killed` rows and fabricating watchdog progress. The evidence boundary now rejects an already-recorded dead GUID before changing metrics, writing telemetry, or returning early, allowing anchor verification to proceed on the same tick.
- Run 018 reduced the GUID-14 duplication to one row per observing bot, but a second bot could still record the same generation kill and the target-search path continued walking the tank toward the confirmed-dead corpse. It stopped in generation 1 with five counted kills over four GUIDs and a death loop. Kill evidence is now generation-global, and confirmed-dead search targets are discarded before corpse approach logic.
- Run 019 verified unique kill GUIDs and exact terminals through the first two packs, then was intentionally stopped when the route strategy was corrected: the optional antechamber Giant should not be killed. Corborus now uses the open east side `(1182, 960, 283.89)` as its tank pull anchor, over 40 yards from the Giant, and 42810 is no longer part of the antechamber pack.
- Runs 020-021 verified the first exact terminal, then exposed next-node target leakage rather than a pathless current pack member. Generation 2 had already killed its four unique members; GUID 56 was the generation-3 source exactly 75.85 yards from the generation-2 anchor, outside its 35-yard radius, but the shared entry 42696 let it hijack target search. Script-target identity is now scoped by current node map, center, and radius. Legal visible DB-profile ranged pulls remain available for genuinely pathless members inside the current cluster.
- Run 022 proved 21 unique kills and exact terminals for generations 1-4, then exposed an invalid east pull anchor at `(1182, 960)`. The replacement `(1128.04, 966.894, 284.703)` is a world-DB-proven reachable floor point, 29.8 yards from Corborus for a legal paladin ranged pull and 43.2 yards from the optional Giant.
- The same audit found a forbidden activation fallback that summoned the route boss when the real instance target was not immediately visible. That fallback and its faction/attack forcing are removed: only the tank may activate a boss, only after reaching the pull anchor, and instance `SetData` must expose the real encounter target.
- Trash routing is migrating from enumerated pack paths to discovery. Stage A will persist generation-scoped GUID cohorts and death evidence while existing nodes seed them; Stage B will replace Stonecore pack coordinates/entry lists with navmesh-corridor discovery legs between strategic boss/regroup anchors; Stage C will migrate the remaining dungeon legs. Evade, path, LOS, selectability, and object-resolution failures never imply member death.
- Stage A is implemented: selected and threat-linked natural mobs join a generation-scoped GUID cohort; each GUID must have cohort-specific engagement before its death is accepted; unresolved members remain live; clearance requires the whole party out of combat plus a two-second quiet window. Trash-time per-target `AttackStop`/`CombatStop` paths and the arbitrary empty-entry nearby-mob fallback are removed.
- Stage B now covers the Corborus approach leg. Its explicit `discovery_leg` manifest has no mob entries, radius, or expected count. The tank projects natural hostiles onto the 3D navmesh path to the strategic endpoint, selects the earliest aggro-envelope intersection with a deterministic GUID tie-break, and persists the discovered GUID. Invalid/incomplete paths fail closed. Boss activation is restricted to eight yards from the pull anchor, avoiding the prior 40-yard activation boundary beside the optional Giant.
- The generated `dataset/validation_scenarios` output was reproduced under DVC and now contains the discovery leg; the stale radius/entry manifest cannot silently select the old behavior.
- Run 023 validated the strict cohort boundary on exact revision `bca870c928`: generation 1 enrolled five engaged GUIDs, recorded four real deaths, and correctly refused to terminal while the fifth remained unresolved by death evidence. The fifth was Millhouse Manastorm 43391, whose real script transitions below 50% to passive Blur 81216 and retreat rather than death. Runtime does not yet parse `scripted_event_entries`, so the valid scripted transition could not satisfy the node and the run ended at the 900-second wall-clock budget with four kills, zero teacher kills, and zero false terminals.
- Run 023 also proved the semantic plateau watchdog is counting repeated unchanged-health `validation_route_combat_progress` diagnoses as progress. Kill count and Millhouse health were unchanged for over 12 minutes, but the no-progress watchdog did not fire. The repair must count actual health improvement after the latest failure event, not repeated diagnosis rows.
- Scripted-transition evidence is now manifest-driven for Millhouse's three flee stages (route steps 1-3): runtime parses aligned entry/aura metadata, requires prior generation-scoped engagement, Blur 81216, no victim, and passive state, and records transition evidence separately from deaths. Generic unattackability, evade, path/LOS failure, or unresolved state cannot satisfy it. Step 4 has no flee contract because the third transition has already moved Millhouse to the final group.
- The plateau watchdog now keys health history by bot, node, generation, target GUID, and entry. Route-progress diagnostics persist their generation, the first health value is only a baseline, and only a strict later decrease counts. Unchanged multi-bot stale snapshots no longer create semantic progress.
- Run 024 proved both repairs on exact revision `775da60fa8`: generation 1 recorded four natural deaths plus one distinct Millhouse transition, and the unchanged-health watchdog later stopped the run with `semantic_progress_plateau_watchdog` rather than waiting for the emergency budget. It also exposed retained scripted focus: transitioned Millhouse remained an eligible authoritative target, so the cohort followed his retreat 329 yards and expanded to 36 engaged GUIDs, 22 deaths, and one transition under generation 1. No false terminal or teacher kill occurred.
- A semantic hard mask and pursuit release is now implemented for completed scripted GUIDs: runtime rejects them from target eligibility, clears only matching shared/per-bot focus and progress GUIDs, cancels only matching active pursuit movement, and preserves member/engagement/transition evidence plus every other live combat target. It does not call `AttackStop` or `CombatStop`.
- Run 025 proved the pursuit release on exact revision `4b8374aeaf`: generation 1 advanced after its bounded natural deaths plus one transition; generation 2 observed its own Millhouse transition and returned to its anchor instead of following him downstream. The next truthful failure is seven unresolved generation-2 members: 15 engaged GUIDs produced seven deaths and one transition, then no target/kill progress. The repaired watchdog stopped at `semantic_progress_plateau_watchdog` after 180 seconds with 11 total kills, zero teacher kills, and no false terminal.
- The unresolved run-025 members came from asymmetric filtering: engagement enrollment omitted `IsSummon()` while every target-selection predicate rejected summons. Enrollment now excludes summons like the shared natural-creature contract. Natural unresolved members still block. New `validation_route_pack_enrolled` telemetry records each new member/engagement with GUID, entry, enrollment reason, and raw state so count-only ambiguity cannot recur.
- Run 026 verified summon exclusion on exact revision `bc797b9474`: generation 1 advanced with four real deaths plus Millhouse's scoped transition; generation 2 enrolled 16 members, recorded ten real deaths plus one transition, and correctly held five unresolved members until the semantic plateau watchdog fired. Character telemetry for run ID 650 identifies the unresolved GUIDs as 12, 15, 28, 29, and 51, all natural Unbound Earth Ragers 43662 threat-linked within 1.6-14 yards. Runtime only observed deaths for the current target or victim, so it could not distinguish a collateral dead member from a live-but-ineligible or missing member; no absence, despawn, evade, or unattackability was accepted as terminal evidence. The artifact is checkpointed as `stonecore_strict_repair_run_026.dvc`.
- Run 027 on exact revision `f2e0befe1d` proved cohort-wide corpse observation: generation 2 enrolled 17 members and produced 16 resolved-corpse deaths plus one scoped Millhouse transition, including all ten Unbound Earth Ragers. It then exposed two new defects. A confirmed-dead pack entry was classified as an activation target before the dead-state branch, preventing terminalization despite complete ledger evidence. The worldserver later exited with SIGSEGV; the corpse sweep iterated the live GUID set while canonical death recording could re-enter cohort enrollment and mutate that set. The run is failed evidence only, with one bot death, zero boss kills, zero teacher kills, and no false terminal, checkpointed as `stonecore_strict_repair_run_027.dvc`.
- Run 028 on exact revision `58a1fddecf` verified the stable corpse snapshot and dead-before-activation ordering: generations 1 and 2 both emitted exact trash terminals; generation 2 had a complete 16-death plus one-transition ledger and advanced normally. The process then exited with SIGSEGV immediately after entering generation 3, the first `discovery_leg`, before selecting a discovered target. This isolates the remaining process fault to discovery-leg execution rather than the repaired cohort ledger. The failed artifact has zero teacher kills and no false terminal and is checkpointed as `stonecore_strict_repair_run_028.dvc`.
- Debug run 029 executed the same exact binary under GDB and reproduced a process exit during generation 2. The validator's early process-exit branch did not drain the debugger's remaining stdout, so the final signal/backtrace was not retained. The failed reproduction is checkpointed separately as `stonecore_strict_repair_run_029_debug.dvc`; the next debugger wrapper must log directly to a file outside the validator pipe.
- Debug run 030 persisted GDB output directly and captured the exact discovery fault: thread 1 received SIGSEGV in `Object::GetGuidValue(this=0x0)`, called by `findTrashClusterThreatTarget` at `BotWorldPopulationMgr.cpp:7448`. The discovery-only membership branch dereferenced `creature->GetGUID()` before rejecting non-creature world objects; non-discovery legs had hidden the defect through short-circuit evaluation. The artifact includes `gdb_backtrace.log` and is checkpointed as `stonecore_strict_repair_run_030_debug.dvc`.
- Run 031 on exact revision `f9e75491f6` verified the discovery crash repair and selected/killed a natural generation-3 cohort from the navmesh corridor. Generations 1 and 2 terminaled truthfully; generation 3 engaged six members and completed all six with five deaths plus Millhouse's transition. It correctly did not terminal because two earlier path candidates had been inserted into the member ledger by selection alone and never engaged. The semantic plateau watchdog stopped the run with 20 real kills, one bot death, zero teacher kills, and no false terminal. The failed artifact is checkpointed as `stonecore_strict_repair_run_031.dvc`.
- Run 032 on exact revision `7d449a5d0a` verified engagement-only membership: the discovered generation-3 cohort terminaled and advanced without speculative path candidates in its ledger. Generation 4 then killed all nine natural trash members but stale cross-node threat re-enrolled Millhouse after his third and final verified retreat; the server script makes him unkillable and leaves him awaiting the Corborus intro event. The semantic plateau watchdog stopped with 27 real kills, zero bot deaths, zero teacher kills, no Giant evidence, and no false terminal. The failed artifact is checkpointed as `stonecore_strict_repair_run_032.dvc`.
- Run 033 on exact revision `535917cce5` verified scenario-final scripted GUID masking: generation 4 enrolled exactly nine natural members and recorded nine real deaths, with no Millhouse re-enrollment and no Giant evidence. Four bots retained stale `IsInCombat()` flags for over 180 seconds despite having no victim, target, or combat action, so the all-party combat-flag check prevented the otherwise complete ledger from terminaling. The semantic plateau watchdog stopped with 31 real kills, zero bot deaths, zero teacher kills, and no false terminal. The failed artifact is checkpointed as `stonecore_strict_repair_run_033.dvc`.
- Run 034 on exact revision `91f3c1173f` verified terminal-only stale combat cleanup: generations 1-4 all emitted truthful terminals and the runtime activated the real Corborus GUID 85 with no Giant evidence. The north-side anchor `(1128.04, 966.894)` remained out of line of sight after the intro and the party died nine times at that point, producing a truthful `validation_route_death_loop` with no boss kill or assistance. A world-DB floor point at `(1103.9, 864.733, 287.98)` is on the boss side of the rock door, 52 yards from Corborus's post-intro home and 84 yards from the optional Giant, and is the next pull anchor. The failed artifact is checkpointed as `stonecore_strict_repair_run_034.dvc`.
- Run 035 on exact revision `edeffc3322` verified the boss-side floor anchor and advanced all four pre-Corborus trash generations with 33 real kills. The real Corborus GUID 85 was engaged and observed falling to 61.50% health, with zero teacher kills and no Giant evidence. The strict watchdog stopped on `validation_route_death_loop` after 14 deaths. Exact run-659 telemetry shows the whole party died around `(1121.46..1126.01, 869.95..870.96)` while targeting Rock Borers 43917; the listed-add scan reached 49 live adds despite 16 observed add kills. This is a high-density add-response failure, not a trash terminal or pull-anchor failure. The failed artifact is checkpointed as `stonecore_strict_repair_run_035.dvc`.
- The discovery audit confirms that only the current Corborus-approach node discovers packs from the live navmesh corridor; the remaining trash nodes still encode pack centers, radii, and entry lists. The next migration keeps only strategic boss/traversal endpoints, discovers the earliest unavoidable hostile whose aggro envelope intersects the corridor, enrolls only combat-linked social members, and processes multiple pack subgenerations inside one route node before terminaling at the endpoint. This deliberately skips the optional Giant when its aggro envelope does not intersect the southwest route.
- Commit `2c35e2844e` implements the first complete discovery corridor: the four pre-Corborus trash nodes are replaced by one strategic endpoint with no trash GUIDs, entries, centers, radii, or expected counts. Each engaged social pack has its own sequence and truthful ledger terminal, then the same route leg resumes. The node advances only after at least one completed pack, no unresolved/current/forward target, no real combat linkage, and every expected bot is alive in the original instance at the endpoint. Repeated Millhouse retreats are resolved from the live manifest aura/passive state rather than globally masking his GUID.
- Commit `0e21e45218` adds a generic high-density boss-add mode. Exact listed-add density is passed into the deterministic DB profile resolver; `min_enemies`/`max_enemies` remain hard gates and density mode permits only legal AoE/cleave candidates. Ranged damage bots move away from the local add centroid during an alive-but-unattackable configured boss phase. A missing legal area action emits an explicit hold and telemetry event; density mode has no pull or single-target fallback.
- Review found and repaired four pre-run blockers. Commit `2e657d739a` preserves final scripted-transition evidence across discovery subpacks and promotes it to the persistent GUID mask only at the truthful corridor terminal. Commit `32576b99a6` uses the correct relative escape angle, rejects stale shared add focus through the same live-wave predicate, and extends the DB taxonomy with explicit `resource_generator` candidates. Density mode admits only AoE, cleave, or declared resource generators; a legal area action always outranks a generator. Marksmanship Steady Shot and Enhancement Stormstrike are explicitly migrated as generators, so the latter can build Maelstrom for Chain Lightning without a hidden single-target or pull fallback.
- Run 036 on exact revision `49349dba1a` proved the merged corridor live: seven dynamic pack sequences produced 21 real deaths plus three separately scoped Millhouse transitions, zero bot deaths, zero Giant 42810 evidence, and no false route terminal. After the seventh pack, all five bots were alive, moving coherently toward the endpoint at roughly seven yards per tick, with the tank shrinking its remaining distance from 59.26 to 19.38 yards in five seconds. The validator nevertheless stopped on `validation_route_stuck_loop`: it summed the same six historical stuck events from status, summary, and trace into 12, then a single new moving-state healer event arrived before the next progress sample. Historical failures had already been resolved by kills, pack terminals, and movement. The runtime route is sound; the watchdog must deduplicate evidence and count only unresolved same-node/generation failures after the latest strong progress.
- Commits `5765ea86b8` and `c54278b1cb` repair stuck-loop accounting: redundant status/summary/trace views use their maximum rather than a sum, only unresolved failures after same-scope strong progress count, and timestamp-only/sequence-only relations are incomparable and fail closed. Dynamic pack terminals and explicit movement/combat blocker resolutions reset only earlier comparable failures in the same node and generation.
- Run 037 on exact revision `c54278b1cb` truthfully terminaled the discovery corridor after 20 real trash kills and three Millhouse transitions, advanced to Corborus, and kept Giant 42810 at zero references. Corborus GUID 85 fell to 58.67-64.65% before the first wipe; the retry GUID 551 fell from roughly 100% to 66.50-68.30%. The validator still stopped on `semantic_progress_plateau_watchdog` because its health detector accepted only reason `route_target_combat_progress`, while boss snapshots used `route_target_no_health_progress` or `boss_route_no_health_progress`. Density behavior also lasted only a few ticks because submerged Corborus temporarily became unresolvable, despite 17-50 listed adds remaining live. The run had six report deaths, no boss kill, no assistance, and no false terminal.
- Commit `6b889bdf5c` replaces the reason/per-bot health detector with party-shared, ordered boss attempt minima scoped by node, generation, GUID, and entry. Same-scope failure/death/wipe and safe near-full resets establish new attempt baselines; only strict new minima count, while repeated health, healing oscillation, duplicate bot snapshots, and incomparable clocks do not. A real 180-second plateau now receives an explicit failure label.
- Commit `aae1050fe2` latches high-density behavior to the current boss node/generation after observed real boss engagement. A temporarily unresolved boss remains unavailable while at least three listed adds are live; the latch clears on boss return, real kill, swarm end, node reset, or generation change. No encounter IDs, coordinates, or fallback actions were added.
- Review commits `08db528a1b` and `788f6dd5aa` close two false-progress/latch races: ordinary stuck, repath, and target-loss events no longer reset boss attempt minima, while death, repeated death, raid wipe, and instance reset do; a local visibility gap cannot clear the shared density phase because unlatching uses a GUID-deduplicated cohort-wide listed-add scan.
- Run 038 on exact revision `9ada78f33d` proved attempt-aware boss progress and density persistence: the corridor terminaled with 17 real trash kills, zero Giant evidence, and Corborus reached 49.88% on the first attempt and roughly 64.51% on the retry. The validator recorded 124 strict boss-health progress steps and did not plateau falsely. Both attempts then collapsed during listed-add swarms, producing 12 deaths and a truthful death-loop failure. Exact masks show the shared single-add focus was usually outside tank/Enhancement range despite dozens of local adds; ranged centroid movement was reissued until players spread up to 88 yards from the tank, while the excluded healer remained in the swarm and died first in both waves. The density phase is now truthful; action anchoring and movement cohesion are the next runtime blockers.
- Commits `a0c213a908`, `366fa5656a`, and `5a06e7b603` implement and harden the generic density repair. Latched density combat uses deterministic per-bot manifest-listed anchors inside DB profile range; an out-of-band nearest add becomes an explicit range-approach candidate, never a pull or single-target fallback. A tank-authoritative, generation-scoped shared escape point is bounded by the actual living healer's range/LOS and an anchor leash; urgent healing precedes escape, movement is issued once per point, and stalled/misdirected paths are verified and reissued. Escape state clears safely when role anchors disappear, while density lifecycle resets remain tied to swarm end, boss return/kill, or route reset. Independent review approved the combined series.
- Run 039 on exact revision `40905ecdc5` reached the Corborus node with 20 real trash kills and zero deaths, then exposed a pre-combat handler regression: when density was false and no listed add existed, the new `no_compatible_density_anchor` branch still consumed every non-healer tick with `hold_boss_add_density`, so activation remained false and the semantic watchdog correctly stopped the run. Commit `be87b7dbcd` releases the add handler immediately for inactive density/no-add state; explicit approach/hold remains active-density-only.
- Run 040 on exact revision `7eb7980498` proved the cohesive repair: Corborus stayed on real GUID 85 and reached 4.622% health without an encounter reset; 31 distinct Borers died in the first add wave versus five across both run-038 waves; selected area/generator actions rose to 39, deaths fell to nine, and stuck/failure telemetry fell to zero. All five bots recovered after the final death cluster and remained active, but the validator stopped on `validation_route_death_loop` because its label used cumulative `deaths >= 8` while watchdog state correctly showed only one of three repeated danger episodes. The first death cluster had already been followed by 58 strict boss-health minima and 14 kills. This is a cumulative-history validator race, not a runtime terminal. The remaining runtime optimization is keeping tank threat pursuit leashed to the shared escape and reducing noncritical heal casts that interrupt healer relocation.
- Commits `7903dd41a8` and `027bc107ce` unify death-loop labeling and watchdog state around three unresolved same-scope `repeated_death`/explicit loop episodes after the latest comparable durable progress. Ordinary cumulative deaths remain outcome data, resurrection alone does not erase danger history, wrong-scope or incomparable progress fails closed, and unsequenced repeat rows remain distinct unresolved evidence. Independent review approved the combined semantics.
- Run 041 on exact revision `f636f2ddb2` is the first strict run with two real boss kills. Corborus GUID85/entry43438 died at 05:05:36 with matching generation-2 terminal evidence; Slabhide GUID86/entry43214 died at 05:11:12 with matching generation-4 terminal evidence. The run produced six dynamic pack terminals, three real Millhouse transitions, truthful post-Corborus trash clearance, and scoped arrival advances through the lower approach. Pre-Corborus Giant evidence remained zero; two Giants were killed only after Corborus. Teacher/forced assists remained zero. The 900-second emergency wall clock stopped generation9/index8 of13 while the sentry gauntlet was actively progressing: DB kills continued to the final second, current target remained at roughly 58%, and no unresolved stuck/death loop existed. The same exact runtime needs a 1,800-second completion budget rather than another behavior change.
- Run 042 on exact revision `979d10d7cb` used the 1,800-second budget and again produced real Corborus and Slabhide kills, then reached the lower sentry gauntlet. Generation9 ended with 31 enrolled/engaged members and 31 real deaths, all five bots alive/out of combat at the anchor, but terminal verification never ran. The node retained `validation_route_progress_baseline_kills=29` across an experiment-run segment while current metrics reset to four, making the redundant global predicate `current kills > baseline` false despite complete generation-scoped evidence. A repeated dead source-entry scan added noise. The repair must enter terminal verification from current-generation observed engagement while retaining broad live-mob, no-combat, full-cohort, and stability checks; blocker GUID/state telemetry will distinguish any subsequent live outsider from an eligibility mismatch.
- Commits `eeec119881` and `4b2cd956f2` remove the resettable global kill baseline from fixed-trash terminal entry while requiring current generation and observed engagement. Broad live-mob veto, party combat, full-cohort endpoint, and two-second stability checks remain unchanged. Already-recorded current-generation corpses no longer prevent empty-search misses from maturing, and every hold now reports the exact failed predicate/timing with a mob GUID/state object only for a real live blocker. Independent review approved the combined truth boundary.
- Run 043 on exact revision `d411d91087ca` killed Corborus truthfully, then failed because hunter 1265 remained roughly 228 yards behind while `follow_tank` and truthful `hunter_pet_missing` readiness repeated. Its persisted pet existed only in a stable slot with no active store row or live GUID, so readiness returned before follow movement. The 713,063,480-byte failed artifact is checkpointed and pushed as `stonecore_strict_repair_run_043.dvc`; its payload and local cache copy were removed after push.
- Commit `56e441ce0d` repairs that load/readiness deadlock without weakening cohort truth. A loadable stored hunter pet is staged into an unused active slot, `LoadPet()` must produce the matching pet number before the database promotion is persisted, and invalid or failed candidates remain missing. The same commit provisions the Stonecore healer as a legal 41-point Holy priest with DBC-derived specialization spells and valid glyph slots, verifies the runtime talent tree/talent/known-spell rows, and makes the dungeon healer resolver DB-authoritative with explicit injured-count, mana, attacker, movement, effective-heal, and overheal gates plus verified Holy emergency/self-survival/threat/mana candidates. Full cast-outcome telemetry remains in progress.
- Commit `272ecb3cab` reproduces the validation gear, provisioning, verification, and scenario stages with explicit Talent/Glyph DBC dependencies. The verifier reports `all_passed=true`; five generated files were pushed to DVC.
- Runs 001-022, including aborted runs 017 and 019, are committed as DVC pointers and pushed. Their working copies and the local DVC cache were removed after push; use `pixi run dvc pull <pointer>` to restore one.
- Run 044 truthfully killed Corborus but exposed normal distance dismissal of the hunter pet; the saved pet became inactive and the hunter stalled behind the cohort. Commit `249864688a` recovers a valid saved active-slot hunter pet through the normal Call Pet spell path without direct summoning or fabricated readiness. The failed run is pushed as `stonecore_strict_repair_run_044.dvc`.
- Run 045 on exact revision `249864688a7b` verified Call Pet recovery and kept the hunter with the cohort through three real boss kills. It plateaued before Azil because runtime attempted a partial path to the boss spawn at `(1337.3, 964.894, 214.2383)`. The failed run is pushed as `stonecore_strict_repair_run_045.dvc`.
- Run 046 on exact revision `311cc41bee34` moved provisioning start to the reachable flayer approach but exposed that `bot_start` is not the runtime navigation coordinate. The manager still targeted the off-navmesh boss spawn, so no Azil pull occurred and the run truthfully stopped at three boss kills. The failed run is pushed as `stonecore_strict_repair_run_046.dvc`.
- The route schema now separates authoritative objective coordinates from navigation anchors. Azil retains source entry 42333 and its world-DB boss spawn while runtime navigation uses `(1329.93, 985.712, 207.804)`. Generated manifests always materialize anchor fields; malformed explicit anchors fail scenario readiness, and older manifests fall back to their existing route coordinates.
- Bounded run 047 exercised the new anchor through the real worldserver under an 8 GiB/three-CPU systemd scope. Runtime diagnostics used the staging destination, discovered authoritative Azil GUID 125/entry 42333, entered normal combat, and reduced her to roughly 75% health with no partial-path failure. The intentionally short 240-second probe stopped on its strict death-loop watchdog and is not a clear. It is pushed as `stonecore_azil_anchor_verify_run_047.dvc`.
- Run 048 on exact revision `4bb37e462470`, under the same 8 GiB/three-CPU cap, again produced three truthful boss kills and genuinely engaged Azil GUID 392. Azil fell to roughly 61.8%, but a transient focus-resolution gap sent the healer toward the dungeon anchor during fresh authoritative boss-focus memory; that strict path failed at `2026-07-11T19:21:04.760Z`, removing the healer from combat before the DPS collapse. The run timed out without an Azil terminal and is pushed as `stonecore_strict_repair_run_048.dvc`.
- Fresh authoritative-focus memory now prioritizes group healing, then holds briefly while re-resolving the exact focus. It neither follows a non-authoritative dungeon anchor nor paths to raw cached target coordinates; failed bounded recovery clears the memory so ordinary live-anchor regroup can resume on the next tick. Strict path rejection and boss terminal truth remain unchanged.
- Run 049 on exact revision `0cdc055d8c52` verified the healer stayed active through Azil combat, but exposed that tactical profile-range/min-range movement shared the strategic route helper's terminal-on-path-failure behavior. Several DPS acquired Azil GUID 392, then a local partial/no-path tactical reposition permanently marked their generation-13 route state failed while the tank and healer continued. The run timed out with two prior real boss kills and no Azil terminal; it is pushed as `stonecore_strict_repair_run_049.dvc`.
- Strategic navigation-anchor movement now explicitly opts into terminal-on-failure semantics. Tactical combat/profile movement remains strict about path validity and records `tactical_path_rejected`, but it cannot convert a rejected local reposition into a false terminal route failure. Every tactical caller reports hold/rejection rather than claiming successful movement.
- Run 050 on exact revision `01827c943800` verified the tactical split but exposed a hard terminal-evidence gap. Slabhide was still alive at 0.724198% at `2026-07-12T05:57:24.505Z`; the last retained generation-4 action was at `05:57:25.106Z`, and generation 6 appeared at `05:57:26.116Z` without retained generation-4 `boss_killed` evidence. Only Corborus and Ozruk had real kill evidence, Azil later reached roughly 55%, and the party ended in a death loop. The failed run is pushed as `stonecore_strict_repair_run_050.dvc`.
- Commit `d68b8aabdb6c` closes that boundary. `Unit::Kill` notifies the autonomy manager only after `JUST_DIED`, `CreatureAI::JustDied`, and summon/instance death handling. The manager accepts only the configured, generation/instance-bound engaged boss GUID, emits `boss_killed` with `confirmed_unit_death`, and gates manifest advancement on the same provenance. Stale target/victim polling, zero health alone, target loss, despawn, evade, and unselectability cannot establish a boss terminal.
- Run 051 on exact revision `d68b8aabdb6c`, under `MemoryMax=8G`, `MemorySwapMax=2G`, and `CPUQuota=300%`, verified the new surface in live runtime. Azil generation 13 emitted `boss_killed: confirmed_unit_death` and matching `validation_route_terminal: boss_killed`, then manifest completion. The report nevertheless rejected final evidence with `validation_route_stuck_loop`, `missing_node_terminal_evidence`, and `missing_real_boss_kill_evidence`; its watchdog counted zero boss-kill evidence despite the retained confirmed Azil event. This is failed evidence, not a truthful four-boss clear. The 1.796 GB artifact is pushed and locally evicted as `stonecore_strict_repair_run_051.dvc`.
- Commit `a7a0dac92795` persists every generation-scoped terminal and confirmed boss death in runtime status, accepts `confirmed_unit_death` in both live and scenario aggregators, and keeps strict per-node evidence requirements after trace rollover. Run 052 proved the ledger through Corborus, Slabhide, and Ozruk: status retained all three confirmed boss deaths and terminals for generations 1-11. It then plateaued on generation 12 twilight flayer packs after 35 deaths, before Azil, so it is failed evidence only. The report initially omitted Slabhide because generic summary payloads could supersede the real status row; status classification now requires `active` or both population fields. The 1.515 GB run is pushed and locally evicted as `stonecore_strict_repair_run_052.dvc`.
- Commit `e0553ccf4b84` routes a cleared, out-of-combat trash cohort to the exact route endpoint instead of holding followers around the tank outside the terminal radius. Run 053 on that exact revision is the first accepted truthful natural four-boss Stonecore clear: all 13 generation-scoped terminals, confirmed `Unit::Kill` evidence for Corborus, Slabhide, Ozruk, and Azil, final scoped manifest completion, and zero teacher/forced kills. Historical stuck/no-progress labels are retained separately as superseded diagnostics only after the complete strict ledger proves resolution. The 1.808 GB run is pushed and locally evicted as `stonecore_strict_repair_run_053.dvc`.
- Run 054 failed after the 1,800-second emergency cap left Azil only about 49 seconds; she remained alive at roughly 55.6% and was still making natural health progress. Series runs now use a 2,400-second emergency cap while retaining the semantic no-progress watchdog. The failed run is pushed and locally evicted as `stonecore_clear_series_run_01_054.dvc`.
- Run 055 is an accepted truthful clear with the complete strict ledger, four confirmed natural boss deaths, no failure labels, and no teacher/forced kills. It established clear 1/10, but the subsequent failures broke that streak. The accepted run is pushed and locally evicted as `stonecore_clear_series_run_01_055.dvc`.
- Run 056 failed on Ozruk at a best observed 3.65% health. Generic `movement_check` handling treated every timed cast as ground danger and issued 587 jumps, scattering the party and suppressing effective healing. Commit `3588fe15b8` now permits that dodge only when `SpellLooksLikeGroundDanger` classifies the active cast. The failed run is pushed and locally evicted as `stonecore_clear_series_run_02_056.dvc`.
- Run 057 on exact revision `3588fe15b8dc` confirmed Corborus, Slabhide, and Ozruk, then failed generation 13 with Azil alive at a retained minimum 46.7601%. A raw strategic anchor at `(1329.93, 985.712, 207.804)` failed `route_destination_invalid_z_transition`, terminalizing the tank while the healer and one DPS held without valid profile actions and two DPS recovered from death. The run accumulated 237 deaths and ended on the no-progress watchdog. Its 3.072 GB/85-file artifact is pushed and locally evicted as `stonecore_clear_series_run_02_057.dvc`. The active uninterrupted streak is therefore 0/10.
- Strategic validation-route anchors are now terrain-projected before strict point/path validation, reusing the existing tactical projection policy while retaining terminal-on-failure semantics if the projected destination still fails. This is generic navigation repair, not Azil-specific behavior, and does not weaken navmesh legality or terminal truth.
- Reusable live-validation sessions now keep one exact-HEAD, fingerprinted `worldserver` in a user systemd unit bounded by `MemoryMax=8G`, `MemorySwapMax=2G`, and `CPUQuota=300%`. An environment/repository file lock serializes the complete stop/inactive/reset/provision/start/watchdog/final-stop interval; SQL cannot run until status proves autonomy inactive with zero active bots. Every attempt uses a new evidence directory, and binary/config/HEAD/profile/route fingerprint changes restart the stable unit. Immutable report processing may overlap the next live attempt, while DVC mutation/push uses a separate repository lock.
- Bounded probes 058 and 059 verified process reuse end to end on exact revision `fca37a4f3f5b`: both used server PID 1213348, probe 059 reported `already_healthy`, and both recorded `inactive_before_preparation=true`, `active_after_start=true`, and `inactive_after_attempt=true`. The unit exposed the required 8 GiB memory, 2 GiB swap, and 300% CPU limits. These short probes are not clears; their artifacts are pushed and locally evicted as `stonecore_session_reuse_probe_058.dvc` and `stonecore_session_reuse_probe_059.dvc`.
- Run 060 on exact revision `069c246b5d69` confirmed Corborus, reached Slabhide generation 4, and observed the real Slabhide GUID six times as `target_seen_not_attackable` after one scripted activation. The watchdog stopped about six seconds into the asynchronous intro because 15 unresolved approach-history stuck events were classified immediately; Slabhide never entered combat and no truthful health minimum was observed. Terrain projection was not the cause: manifest Z `247.6368` projected to runtime Z `247.604`, with no generation-4 path rejection. The run is failed evidence, does not count, and is pushed and locally evicted as `stonecore_clear_series_run_01_060.dvc`.
- Commit `306127c329` adds a bounded scripted-activation watchdog grace without changing runtime terminal truth. It suppresses only the immediate `validation_route_stuck_loop` machine label for at most 30 seconds when a unique maximum unresolved scoped history is followed by a same-scope activation and a later positive-GUID `target_seen_not_attackable` observation. Activation never resolves stuck/death history, boss completion, or route terminal evidence; wrong-scope, unscoped, tied, malformed, silent, or expired evidence fails closed. Independent Sol review found no surviving issue.
- Run 061 used one fresh resource-capped reusable session and truthfully confirmed Corborus, Slabhide, and Ozruk before one generation-13 Azil pull. Azil fell continuously to `45.7362%`; the party then wiped once, healer third, with no second pull or health reset. Generation 13 records 27 failed `safe_local` recovery cycles, zero successful resurrection after the Azil wipe, 22 repeated-death events, 16 tactical partial-path rejections near the route anchor, and no successful interrupt. Source diagnosis found that each failed recovery reset `DeadTimer` to the same value used to detect a fresh death, so one unchanged corpse was re-recorded every five seconds and repeatedly poisoned its local recovery area. The generic repair records each alive-to-dead episode once while retaining an independent recovery retry timer; successful recovery or a later observed alive tick rearms real death accounting. The same repair corrects validation-route hazard escape to use a bot-relative away bearing and reports rejected tactical movement truthfully. The watchdog truthfully stopped on `validation_route_death_loop`; all cleanup boundaries held, including `inactive_after_attempt=true`. No evidence indicates worldserver or instance reuse caused the failure. The run is failed evidence, does not count, and is pushed as `stonecore_clear_series_run_01_061.dvc`; its payload was evicted after checkpointing. The active uninterrupted streak remains 0/10.
- Run 062 on exact revision `175aec72f417` confirmed natural Corborus, Slabhide, and Ozruk deaths and exact terminals through generation 11, then stopped in generation 12 before Azil. Boss combat durations were far below the expected throughput for Dragon Soul gear: Corborus `121.272s`, Slabhide `113.937s`, and Ozruk `124.876s`. Rotation audit found a false 12-yard minimum on all Fire actions and invalid `requires_instant_cast` flags on Marksmanship Chimera Shot, Aimed Shot, and Steady Shot; Fire now uses legal native zero-minimum spell ranges while Marksmanship retains the real five-yard ranged-weapon dead zone and allows its cast-time shots. The generation-12 failure was a poisoned pack ledger: the 120-yard threat scan enrolled 59 creatures without the node entry/radius boundary, only 22 died, and 37 unresolved members blocked terminal truth despite no eligible focus. New threat-linked enrollment now requires the same live objective boundary; unresolved legitimate members still block and no missing member is inferred dead. Run 062 does not count, so the active streak remains 0/10. Its 1.149 GB/53-file artifact is pushed as `stonecore_clear_series_run_01_062.dvc`; the materialized payload and remotely backed local DVC cache were evicted after diagnosis. The stale reusable server was stopped.
- Run 063 on exact revision `e834e640482a` verified the reviewed Fire range, Marksmanship cast-directive, Enhancement melee-archetype, and bounded threat-enrollment changes were live, but failed in generation 1 after 18 real kills and four real deaths. The tank, healer, Fire Mage, and Marksmanship Hunter each produced exactly one truthful death episode; Enhancement remained alive. Every corpse then retried `safe_local` recovery every five seconds without success. The first death row incorrectly stored both `death_count=1` and `failure_count=1`, so the local danger score `death_count * 2 + stuck_count + failure_count` immediately reached the recovery rejection threshold of three and made every genuine first death prohibit its own local recovery. The remaining Enhancement bot produced target-not-found/anchor holds while party combat remained active, so the corridor could not terminal. The narrow repair records a death only as `death_count`, leaving `failure_count=0`; a first death scores two and may use existing safe-local recovery, while a repeated nearby death count of two scores four and remains blocked. This changes no terminal, boss-death, path-legality, or fallback truth boundary. The run is failed evidence and the streak remains 0/10. Its artifact is pushed as `stonecore_clear_series_run_01_063.dvc`; remote sync was reverified after diagnosis, the restored 965 MB payload and remotely backed cache were evicted again, and the server remains stopped.
- Run 064 on exact revision `e9fa96ea8ec2` ran for the full 2,400-second capped window with all five bots alive and zero deaths, proving the first-death recovery repair introduced no fabricated death or fallback evidence but did not reach that path. Generation 1 enrolled 14 objective-bounded members and retained 13 natural deaths plus one verified Millhouse transition. One enrolled member remained unresolved while no eligible target could be found; all five bots stayed in combat roughly five yards from the corridor endpoint and repeated target-search/anchor holds until the emergency cap. The watchdog did not falsely classify a death, stuck, repeated-decision, or semantic-progress loop, and terminal truth correctly remained incomplete. The retained artifact exposed only aggregate ledger and combat counts, so no exact unresolved GUID/state could be established without inference. Commit `c1917e2280` adds read-only `.botauto diagnose` evidence for every enrolled member's encoded identity, observed/live/attackable/evade and ledger outcome state, plus deterministically ordered per-bot victim/attacker GUID links. It changes no target selection, terminal predicate, combat state, or fallback behavior. Run 064 is failed evidence, so the streak remains 0/10. Its 2.8 GB/82-file artifact is pushed as `stonecore_clear_series_run_01_064.dvc`; remote sync was verified, the payload/cache were immediately evicted, and the resource-capped server was stopped.
- Run 065 on exact revision `c1917e228038` completed four truthful discovery-pack terminals in generation 1 with 16 natural kills. Two real bot death episodes recovered successfully, providing initial live confirmation that first-death local recovery can now succeed without fabricated repeats. The final pack ledger was empty and all five diagnostic combat-link arrays showed no victim or attackers. Every bot was alive and out of combat, but the hunter remained `23.0603` yards from the endpoint because `hunter_pet_dead` readiness returned before the existing cleared-trash convergence branch; the other four were within 12 yards. The strict 18-yard full-cohort terminal therefore correctly held for roughly 1,960 seconds. Commit `3b1e504fa5` moves only cleared, out-of-combat non-tank endpoint convergence before readiness, while retaining ground-danger precedence, active tank/authoritative focus guards, strict path validation, and every terminal predicate. Run 065 is failed evidence, so the streak remains 0/10. Its 2.6 GB/82-file artifact is pushed as `stonecore_clear_series_run_01_065.dvc`; remote sync was verified.
- Run 066 on exact revision `3b1e504fa58c` proved the endpoint convergence repair: all five bots reached within `0.0836` yards of the corridor endpoint. It nevertheless failed generation 1 after one completed discovery pack and 15 natural kills. Five truthful death episodes occurred; the healer and two DPS visibly recovered through `safe_local`, while the tank remained dead and retried recovery 321 times through the emergency cap. Final diagnostics show an empty pack ledger, no combat links, and no false terminal; the recovery failure, not endpoint convergence, blocked the cohort. Run 066 is failed evidence, so the streak remains 0/10. Its 2.5 GB/82-file artifact is pushed as `stonecore_clear_series_run_01_066.dvc`; remote sync was verified and the server was stopped.
- Commit `690d88bbd5` adds native party resurrection for headless bots without a custom resurrection or teleport fallback. A living, out-of-combat healer selects an actually known native resurrection spell, obeys normal group/map/instance, range, LOS, power, cooldown, cast-time, spell-effect, and combat-resurrection legality, and casts through `Player::CastSpell`. The dead bot accepts the resulting request through `HandleResurrectResponseOpcode` and acknowledges native near-teleport through `HandleMoveTeleportAck`. Bot-only target gates, victim/caster reservations, and active-cast protection prevent interruption or races with artificial local recovery. Independent high-risk review approved the final implementation.
- Run 067 on exact revision `690d88bbd54c` verified the reusable-session semantic plateau fail-fast path: the persistent resource-capped validator stopped after 423 seconds rather than consuming the 2,400-second emergency cap. Generation 1 produced 14 natural kills and one truthful healer death; `safe_local` recovery succeeded, so native party resurrection was not exercised. The last durable kill was GUID 64 at `2026-07-13T20:10:12.496Z`. The third discovery-pack ledger contained GUIDs 30, 32, 34, 61-65 and truthfully recorded all eight natural deaths, but live combat still linked the cohort to social attackers 9, 31, 56, and 57. Those attackers had appeared progressively from heartbeat 2 onward yet could not enter the self-referential discovery ledger because its enrollment scan accepted only already-enrolled GUIDs. The complete eight-member ledger therefore could not terminal while party combat remained active, and no eligible persisted target existed to reacquire. Commit `9aae95cf7c` repairs that exact blocker generically by enumerating active unsuppressed same-instance PvE `CombatReference` objects from loaded cohort members, enrolling only natural non-boss creatures, retaining fixed-leg objective boundaries, and continuing to require natural death or a verified scripted transition for every enrolled GUID. It removes the broad 120-yard enrollment scan and stale threat-table lookup; evade, temporary unattackability, path failure, disappearance, and target-selection failure still cannot erase truthful enrollment. The report ended on `semantic_progress_plateau_watchdog`, is failed evidence, and leaves the uninterrupted streak at 0/10. Its 126 MB/24-file artifact is pushed as `stonecore_clear_series_run_01_067.dvc`; remote sync was verified and the server was stopped.
- Run 068 on exact revision `9aae95cf7c6f` provided live acceptance of the combat-reference enrollment repair but exposed the next truthful blocker. It completed three discovery packs and produced 21 natural kills with zero deaths before the semantic watchdog stopped at 636 seconds. The previously omitted social attackers were now enrolled through `cohort_combat_reference`: GUID 31 was naturally killed, GUIDs 56 and 57 were enrolled, reacquired, and naturally killed, and GUID 9 was enrolled and later resolved through its configured scripted transition. Final diagnostics contained 14 genuinely engaged members, 11 natural deaths, one scripted transition, zero cohort combat links, and two remaining alive evade-mode Stonecore bruisers, GUIDs 51 and 52. Both remained truthfully unresolved and blocked the fourth-pack terminal; the repair therefore fixed the run-067 deadlock without weakening ledger truth, but the run is failed evidence and the uninterrupted streak remains 0/10. The 266 MB/31-file artifact is pushed as `stonecore_clear_series_run_01_068.dvc`; remote sync was verified and the resource-capped server was stopped.
- Core evade-lifecycle diagnosis established that generic no-path evade installs `HOME_MOTION_TYPE` and clears `UNIT_STATE_EVADE` only after the home generator arrives. Formation-leader death dismisses living followers through `CreatureGroup::FormationReset(true)`, whose unconditional `MotionMaster::Initialize()` could delete an in-flight home generator before arrival and leave evade state permanently set. Commit `0d1acadc2a95` preserves native home movement for a dismissed follower already returning home and adds read-only pack diagnostics for spawn ID, current/home coordinates, home distance, motion types, returning-home state, and formation identity. It does not clear evade directly, infer death, alter target legality, weaken the pack ledger, teleport, or force a kill.
- Run 069 on exact revision `0d1acadc2a95` provided live acceptance of the evade repair and exact runtime mapping. GUID 51 was database spawn `340452`, a follower in formation `340450`; it remained alive, attackable, and non-evading during heartbeat 6, then died naturally before heartbeat 7. No entry-43662 member remained in evade. Generation 1 truthfully terminalized after 21 enrolled members resolved; Corborus then died naturally, and generation 3 terminalized after its three engaged members died. The run reached Slabhide generation 4 with 28 natural kills, one confirmed boss death, zero bot deaths, and zero teacher-assisted kills before the machine predicate stopped on `validation_route_stuck_loop` during approach. Thus run 069 is failed evidence and the uninterrupted streak remains 0/10, but the run-068 permanent-evade blocker is repaired. Its 374 MB/35-file artifact is pushed as `stonecore_clear_series_run_01_069.dvc`; pointer-specific remote sync was verified, run 067 was removed only afterward, runs 068 and 069 remain materialized, the DVC cache was pruned to approximately 1.1 MB, and the resource-capped server was stopped.
- Run-069 generation-4 trace diagnosis found a generic update-cadence defect rather than a truthful route deadlock. `BotWorldPopulationMgr::UpdateBot` runs at world-update cadence but treated only a single update displacement of at least `0.2` yards as movement progress. The final diagnoses showed every bot actively moving and four bots covering about `6.84` yards per decision while `time_since_last_progress_ms` still grew beyond 16 seconds; each sub-update displacement remained below the threshold, so blocker resolution never observed the accumulated real transit and the six-second stuck timer repeatedly fired. Commit `ee759ca0efc1` accumulates displacement in a bounded one-second movement window, resolves a blocker only after at least `0.2` yards of measured real movement in that window, and resets both that window and the stuck timer on such progress. It does not treat an issued move command as progress, change the validator threshold, accept historical or wrong-scope evidence, teleport, alter path legality, or weaken route-terminal truth.
- Run 070 on exact revision `ee759ca0efc1` provided live acceptance of the movement-progress repair. Generation 4 produced zero `stuck_detected` rows and zero unresolved route-stuck events, Slabhide died naturally, and its exact generation-scoped terminal completed. The uninterrupted manifest then advanced through eleven route terminals and three confirmed natural boss deaths (Corborus, Slabhide, and Ozruk) with 92 natural kills and zero teacher assistance. Generation 12 exposed the next blocker: the tank, healer, and Fire Mage died truthfully in the twilight-flayer packs and repeatedly failed `safe_local` recovery with `no_recovery_mode_succeeded`; the surviving Hunter was blocked on `hunter_pet_dead` and the Enhancement Shaman held anchor, so the fourth boss was never reached. The final report stopped on `semantic_progress_plateau_watchdog`; this is failed evidence and the streak remains 0/10. Its 1.5 GB/63-file artifact is pushed as `stonecore_clear_series_run_01_070.dvc`; remote sync was verified before run 068 was removed, runs 069 and 070 remain materialized, the DVC cache was pruned to approximately 1.1 MB, and the resource-capped server and temporary credential wrapper were stopped and removed.
- Run 071 on exact revision `a99700999504` tested redundant native resurrection from both the Holy Priest and Enhancement Shaman. It stopped after 849 seconds on `semantic_progress_plateau_watchdog` at route index 2/13 with 22 natural kills, eight truthful bot deaths, one confirmed natural Corborus death, zero teacher-assisted kills, and no successful interrupts. No native resurrection cast was submitted: telemetry recorded 58 `SPELL_FAILED_NOT_SHAPESHIFT` (`70`) results and six `SPELL_FAILED_PREVENTED_BY_MECHANIC` (`150`) results, with no accepted native resurrection request. Local recovery eventually restored the cohort, but the run remained blocked in the crystalspawn corridor. The live evidence therefore rejects the current native-resurrection repair as sufficient and leaves the uninterrupted streak at 0/10. The next repair must classify these permanent cast failures, avoid letting a permanently rejected resurrection candidate monopolize the route handler, and preserve normal spell legality and the prohibition on direct resurrection or teleport fallback.

Verification:

- Focused reusable-session, SOAP watchdog, inactive-state, and run-plan tests pass (`14 passed`); a real session dry-run confirms reset/provisioning remain unapplied and the 2,400-second cap is retained.
- The final scripted-activation grace suite passes (`14 passed, 248 deselected`), including scope, wall-time expiry, malformed evidence, unscoped/tied history, and death-loop non-resolution cases.
- Focused first-death, endpoint-convergence, native-resurrection, and transport semantic-plateau checks pass; the native lifecycle checks include real response/teleport-ack handlers and prohibit direct resurrection or custom teleport fallback.
- The broader focused Python suite currently has nine pre-existing generated-artifact/source-contract failures unrelated to these repairs; the changed contracts pass their focused checks.
- Runs through 071 are checkpointed and pushed to DVC. The failed run 071 remains authoritative evidence for the native-resurrection rejection blocker; materialized experiment payloads may be removed after remote verification and repository integration. Older runs are restored from DVC only when needed and deleted again immediately afterward.

Next handoff:

Continue from failed run 071 after repository integration. Diagnose why native Priest Resurrection `2006` and Shaman Ancestral Spirit `2008` return cast results 150 and 70 respectively, and prevent permanently rejected candidates from monopolizing `TryNativePartyResurrection` while retaining normal spell legality, request ownership, native response handling, and the prohibition on direct resurrection or teleport fallback. Then continue replacing the generic current-party combat policy with explicit Cataclysm 4.3.4 Protection, Holy, Fire, Marksmanship, and Enhancement priorities using legal spell semantics and live action-frequency/boss-duration telemetry. Build `worldserver` sequentially with `-j1`, verify exact embedded HEAD, run the next serialized resource-capped Stonecore attempt, DVC-checkpoint it, verify remote sync, and keep the server stopped during diagnosis and code changes. Any failure resets the streak; do not weaken native legality or terminal truth.
