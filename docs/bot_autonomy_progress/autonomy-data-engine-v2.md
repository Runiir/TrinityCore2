# Autonomy Data Engine v2

## Phase 1: runtime truth repair

Status: implementation complete; live Stonecore gate not yet passed.

Changed:

- Trash liveness is independent from target selectability, evade state, and path availability.
- Trash nodes require observed engagement and verified cluster clearance; `expected_alive_count` is descriptive only.
- Boss nodes require a naturally dead target. Validation force damage, forced death, and teacher completion were removed.
- Route terminals and boss kills are scoped by exact node and generation in runtime traces and report aggregation.
- Stuck recovery requires progress after the latest stuck or target-loss event.
- Full-clear and segment reports reject unscoped counters, stale nodes, forced assistance, missing terminals, and missing per-boss kills.
- Pytest ignores `generated/orchestrator_worktrees`.

Worker routing:

- Large/high-risk diagnosis used `gpt-5.6-sol` with high reasoning.
- Medium implementation and debugging used `gpt-5.6-terra` with medium reasoning.
- Structured SQL/manifest audits used `gpt-5.6-luna` with low reasoning.
- Simple disk/DVC and predicate audits used `gpt-5.3-codex-spark` with low reasoning.
- These were direct worker Codex sessions selected from the orchestration skill; `tools/bot_ml/orchestrator_daemon.py` was never invoked. A later Sol audit launch was blocked by account quota, so the existing collaboration reviewer performed that read-only audit.

Strict live iterations:

- Runs 001-003 isolated motion resets, cohort routing, and overlapping trash scopes.
- Runs 004-005 established exact post-stuck progress and let dungeon cohorts join the tank's authoritative focus.
- Run 006 exposed the three-second combat cadence; the active route/combat cadence is now capped at one second.
- Run 007 reached Slabhide after a real Corborus kill, but its final trace was incomplete and the airborne approach failed strict Z validation.
- Run 008 verified full trace capture and terrain-projected approaches, then wiped at Corborus with the healer dying first at 17.4% boss health.
- Run 009 proved targeted aura movement for Crystal Barrage spell 86881, but also misclassified shared Dampening Wave spell 82415 and caused a manifest-route death loop.
- Run 010 was aborted before Corborus after a DBC audit proved its periodic-trigger refinement would reject 86881. The aborted diagnostic is also checkpointed in DVC.
- The current implementation matches the loaded spell shape: a harmful `PERSISTENT_AREA_AURA` with `PERIODIC_DAMAGE`, and moves away from the aura's dynamic-object owner rather than the boss caster. This accepts 86881, rejects 82415, and remains encounter-ID independent.
- Run 011 verified 20 real 86881 movement events and zero 82415 movement events. The healer was never selected by 86881 but still died first immediately after the boss submerge/add phase, followed by a death-loop watchdog stop.
- The manifest's `adds` tactic now uses a dedicated `add_target_entries` list inside the strict route handler: non-healers select only engaged listed adds with their DB-backed class/spec action profile, while the healer continues triage. Boss legality and kill evidence are unchanged.
- Run 012 proved the dedicated manifest list selects only Rock Borer 43917, never charge hazard 43743 or unrelated creatures. The healer survived the previous failure point, but two of the first three add decisions returned `no_action`; DPS1266 and the tank died next.
- Add engagement now initiates the normal auto-attack/pull alongside the class/spec action, matching the established boss-target path without changing hard masks or terminal evidence.
- Run 013 made every listed-add action succeed, but tank and DPS selected three different borer GUIDs and still wiped. A generation-scoped shared add focus now keeps the party on the first valid listed add until it dies, without touching boss focus or terminal evidence.
- Run 014 verified the listed-entry mask, successful add actions, and coordinated sequential focus: 73 complete-trace add actions covered 29 GUIDs in 29 non-interleaved contiguous blocks, with explicit dead-target evidence on several transitions. The first truthful failure is now add-wave survivability/throughput, not focus divergence. The party stopped on `validation_route_death_loop` with 21 trash kills, 11 deaths, Corborus at a 48.36% minimum, no boss kill, and exact terminals only for generations 1-4. The run contains no forbidden assistance. Its artifact is checkpointed and pushed as `stonecore_strict_repair_run_014.dvc`.
- Run 015 persistently verified 39 real Rock Borer deaths and coherent focus transitions, then exposed the actual overlap: Crystalspawn Giant 42810 at `(1150.24, 929.81)` remained alive beside Corborus because the generation-4 antechamber manifest omitted its entry. Its repeated Quake 81008 mechanics overlapped the boss/add phase before the death-loop stop. The antechamber cluster now includes 42810, so its observed clearance is required before the boss node.
- Run 016 proved that manifest membership alone was insufficient: generation 4 included 42810 but terminaled after 21 other trash kills without a 42810 kill; the same live Giant then emitted Quake mechanics during Corborus. The terminal scan had been allowed before a bot reached the node anchor, so an unloaded nearby grid looked empty. Kill-triggered clearance is now deferred and scan-based terminal requires arrival within the node radius before verifying no live pack member.
- Run 017 was intentionally aborted after the deferred-terminal path repeatedly emitted the same dead GUID 14 from the tank's stale victim, reaching 140 duplicate `mob_killed` rows and fabricating watchdog progress. The evidence boundary now rejects an already-recorded dead GUID before changing metrics, writing telemetry, or returning early, allowing anchor verification to proceed on the same tick.
- Run 018 reduced the GUID-14 duplication to one row per observing bot, but a second bot could still record the same generation kill and the target-search path continued walking the tank toward the confirmed-dead corpse. It stopped in generation 1 with five counted kills over four GUIDs and a death loop. Kill evidence is now generation-global, and confirmed-dead search targets are discarded before corpse approach logic.
- Run 019 verified unique kill GUIDs and exact terminals through the first two packs, then was intentionally stopped when the route strategy was corrected: the optional antechamber Giant should not be killed. Corborus now uses the open east side `(1182, 960, 283.89)` as its tank pull anchor, over 40 yards from the Giant, and 42810 is no longer part of the antechamber pack.
- Runs 020-021 verified the first exact terminal, then exposed next-node target leakage rather than a pathless current pack member. Generation 2 had already killed its four unique members; GUID 56 was the generation-3 source exactly 75.85 yards from the generation-2 anchor, outside its 35-yard radius, but the shared entry 42696 let it hijack target search. Script-target identity is now scoped by current node map, center, and radius. Legal visible DB-profile ranged pulls remain available for genuinely pathless members inside the current cluster.
- Run 022 proved 21 unique kills and exact terminals for generations 1-4, then exposed an invalid east pull anchor at `(1182, 960)`. The replacement `(1128.04, 966.894, 284.703)` is a world-DB-proven reachable floor point, 29.8 yards from Corborus for a legal paladin ranged pull and 43.2 yards from the optional Giant.
- The same audit found a forbidden activation fallback that summoned the route boss when the real instance target was not immediately visible. That fallback and its faction/attack forcing are removed: only the tank may activate a boss, only after reaching the pull anchor, and instance `SetData` must expose the real encounter target.
- Trash routing is migrating from enumerated pack paths to discovery. Stage A will persist generation-scoped GUID cohorts and death evidence while existing nodes seed them; Stage B will replace Stonecore pack coordinates/entry lists with navmesh-corridor discovery legs between strategic boss/regroup anchors; Stage C will migrate the remaining dungeon legs. Evade, path, LOS, selectability, and object-resolution failures never imply member death.
- Stage A is implemented: selected and threat-linked natural mobs join a generation-scoped GUID cohort; each GUID must have cohort-specific engagement before its death is accepted; unresolved members remain live; clearance requires the whole party out of combat plus a two-second quiet window. Trash-time per-target `AttackStop`/`CombatStop` paths and the arbitrary empty-entry nearby-mob fallback are removed.
- Stage B now covers the Corborus approach leg. Its explicit `discovery_leg` manifest has no mob entries, radius, or expected count. The tank projects natural hostiles onto the 3D navmesh path to the strategic endpoint, selects the earliest aggro-envelope intersection with a deterministic GUID tie-break, and persists the discovered GUID. Invalid/incomplete paths fail closed. Boss activation is restricted to eight yards from the pull anchor, avoiding the prior 40-yard activation boundary beside the optional Giant.
- The generated `dataset/validation_scenarios` output was reproduced under DVC and now contains the discovery leg; the stale radius/entry manifest cannot silently select the old behavior.
- Run 023 validated the strict cohort boundary on exact revision `bca870c928`: generation 1 enrolled five engaged GUIDs, recorded four real deaths, and correctly refused to terminal while the fifth remained unresolved by death evidence. The fifth was Millhouse Manastorm 43391, whose real script transitions below 50% to passive Blur 81216 and retreat rather than death. Runtime does not yet parse `scripted_event_entries`, so the valid scripted transition could not satisfy the node and the run ended at the 900-second wall-clock budget with four kills, zero teacher kills, and zero false terminals.
- Run 023 also proved the semantic plateau watchdog is counting repeated unchanged-health `validation_route_combat_progress` diagnoses as progress. Kill count and Millhouse health were unchanged for over 12 minutes, but the no-progress watchdog did not fire. The repair must count actual health improvement after the latest failure event, not repeated diagnosis rows.
- Scripted-transition evidence is now manifest-driven for Millhouse's three flee stages (route steps 1-3): runtime parses aligned entry/aura metadata, requires prior generation-scoped engagement, Blur 81216, no victim, and passive state, and records transition evidence separately from deaths. Generic unattackability, evade, path/LOS failure, or unresolved state cannot satisfy it. Step 4 has no flee contract because the third transition has already moved Millhouse to the final group.
- The plateau watchdog now keys health history by bot, node, generation, target GUID, and entry. Route-progress diagnostics persist their generation, the first health value is only a baseline, and only a strict later decrease counts. Unchanged multi-bot stale snapshots no longer create semantic progress.
- Run 024 proved both repairs on exact revision `775da60fa8`: generation 1 recorded four natural deaths plus one distinct Millhouse transition, and the unchanged-health watchdog later stopped the run with `semantic_progress_plateau_watchdog` rather than waiting for the emergency budget. It also exposed retained scripted focus: transitioned Millhouse remained an eligible authoritative target, so the cohort followed his retreat 329 yards and expanded to 36 engaged GUIDs, 22 deaths, and one transition under generation 1. No false terminal or teacher kill occurred.
- A semantic hard mask and pursuit release is now implemented for completed scripted GUIDs: runtime rejects them from target eligibility, clears only matching shared/per-bot focus and progress GUIDs, cancels only matching active pursuit movement, and preserves member/engagement/transition evidence plus every other live combat target. It does not call `AttackStop` or `CombatStop`.
- Run 025 proved the pursuit release on exact revision `4b8374aeaf`: generation 1 advanced after its bounded natural deaths plus one transition; generation 2 observed its own Millhouse transition and returned to its anchor instead of following him downstream. The next truthful failure is seven unresolved generation-2 members: 15 engaged GUIDs produced seven deaths and one transition, then no target/kill progress. The repaired watchdog stopped at `semantic_progress_plateau_watchdog` after 180 seconds with 11 total kills, zero teacher kills, and no false terminal.
- The unresolved run-025 members came from asymmetric filtering: engagement enrollment omitted `IsSummon()` while every target-selection predicate rejected summons. Enrollment now excludes summons like the shared natural-creature contract. Natural unresolved members still block. New `validation_route_pack_enrolled` telemetry records each new member/engagement with GUID, entry, enrollment reason, and raw state so count-only ambiguity cannot recur.
- Run 026 verified summon exclusion on exact revision `bc797b9474`: generation 1 advanced with four real deaths plus Millhouse's scoped transition; generation 2 enrolled 16 members, recorded ten real deaths plus one transition, and correctly held five unresolved members until the semantic plateau watchdog fired. Character telemetry for run ID 650 identifies the unresolved GUIDs as 12, 15, 28, 29, and 51, all natural Unbound Earth Ragers 43662 threat-linked within 1.6-14 yards. Runtime only observed deaths for the current target or victim, so it could not distinguish a collateral dead member from a live-but-ineligible or missing member; no absence, despawn, evade, or unattackability was accepted as terminal evidence. The artifact is checkpointed as `stonecore_strict_repair_run_026.dvc`.
- Run 027 on exact revision `f2e0befe1d` proved cohort-wide corpse observation: generation 2 enrolled 17 members and produced 16 resolved-corpse deaths plus one scoped Millhouse transition, including all ten Unbound Earth Ragers. It then exposed two new defects. A confirmed-dead pack entry was classified as an activation target before the dead-state branch, preventing terminalization despite complete ledger evidence. The worldserver later exited with SIGSEGV; the corpse sweep iterated the live GUID set while canonical death recording could re-enter cohort enrollment and mutate that set. The run is failed evidence only, with one bot death, zero boss kills, zero teacher kills, and no false terminal, checkpointed as `stonecore_strict_repair_run_027.dvc`.
- Run 028 on exact revision `58a1fddecf` verified the stable corpse snapshot and dead-before-activation ordering: generations 1 and 2 both emitted exact trash terminals; generation 2 had a complete 16-death plus one-transition ledger and advanced normally. The process then exited with SIGSEGV immediately after entering generation 3, the first `discovery_leg`, before selecting a discovered target. This isolates the remaining process fault to discovery-leg execution rather than the repaired cohort ledger. The failed artifact has zero teacher kills and no false terminal and is checkpointed as `stonecore_strict_repair_run_028.dvc`.
- Debug run 029 executed the same exact binary under GDB and reproduced a process exit during generation 2. The validator's early process-exit branch did not drain the debugger's remaining stdout, so the final signal/backtrace was not retained. The failed reproduction is checkpointed separately as `stonecore_strict_repair_run_029_debug.dvc`; the next debugger wrapper must log directly to a file outside the validator pipe.
- Debug run 030 persisted GDB output directly and captured the exact discovery fault: thread 1 received SIGSEGV in `Object::GetGuidValue(this=0x0)`, called by `findTrashClusterThreatTarget` at `BotWorldPopulationMgr.cpp:7448`. The discovery-only membership branch dereferenced `creature->GetGUID()` before rejecting non-creature world objects; non-discovery legs had hidden the defect through short-circuit evaluation. The artifact includes `gdb_backtrace.log` and is checkpointed as `stonecore_strict_repair_run_030_debug.dvc`.
- Run 031 on exact revision `f9e75491f6` verified the discovery crash repair and selected/killed a natural generation-3 cohort from the navmesh corridor. Generations 1 and 2 terminaled truthfully; generation 3 engaged six members and completed all six with five deaths plus Millhouse's transition. It correctly did not terminal because two earlier path candidates had been inserted into the member ledger by selection alone and never engaged. The semantic plateau watchdog stopped the run with 20 real kills, one bot death, zero teacher kills, and no false terminal. The failed artifact is checkpointed as `stonecore_strict_repair_run_031.dvc`.
- Run 032 on exact revision `7d449a5d0a` verified engagement-only membership: the discovered generation-3 cohort terminaled and advanced without speculative path candidates in its ledger. Generation 4 then killed all nine natural trash members but stale cross-node threat re-enrolled Millhouse after his third and final verified retreat; the server script makes him unkillable and leaves him awaiting the Corborus intro event. The semantic plateau watchdog stopped with 27 real kills, zero bot deaths, zero teacher kills, no Giant evidence, and no false terminal. The failed artifact is checkpointed as `stonecore_strict_repair_run_032.dvc`.
- Run 033 on exact revision `535917cce5` verified scenario-final scripted GUID masking: generation 4 enrolled exactly nine natural members and recorded nine real deaths, with no Millhouse re-enrollment and no Giant evidence. Four bots retained stale `IsInCombat()` flags for over 180 seconds despite having no victim, target, or combat action, so the all-party combat-flag check prevented the otherwise complete ledger from terminaling. The semantic plateau watchdog stopped with 31 real kills, zero bot deaths, zero teacher kills, and no false terminal. The failed artifact is checkpointed as `stonecore_strict_repair_run_033.dvc`.
- Run 034 on exact revision `91f3c1173f` verified terminal-only stale combat cleanup: generations 1-4 all emitted truthful terminals and the runtime activated the real Corborus GUID 85 with no Giant evidence. The north-side anchor `(1128.04, 966.894)` remained out of line of sight after the intro and the party died nine times at that point, producing a truthful `validation_route_death_loop` with no boss kill or assistance. A world-DB floor point at `(1103.9, 864.733, 287.98)` is on the boss side of the rock door, 52 yards from Corborus's post-intro home and 84 yards from the optional Giant, and is the next pull anchor. The failed artifact is checkpointed as `stonecore_strict_repair_run_034.dvc`.
- Run 035 on exact revision `edeffc3322` verified the boss-side floor anchor and advanced all four pre-Corborus trash generations with 33 real kills. The real Corborus GUID 85 was engaged and observed falling to 61.50% health, with zero teacher kills and no Giant evidence. The strict watchdog stopped on `validation_route_death_loop` after 14 deaths. Exact run-659 telemetry shows the whole party died around `(1121.46..1126.01, 869.95..870.96)` while targeting Rock Borers 43917; the listed-add scan reached 49 live adds despite 16 observed add kills. This is a high-density add-response failure, not a trash terminal or pull-anchor failure. The failed artifact is checkpointed as `stonecore_strict_repair_run_035.dvc`.
- The discovery audit confirms that only the current Corborus-approach node discovers packs from the live navmesh corridor; the remaining trash nodes still encode pack centers, radii, and entry lists. The next migration keeps only strategic boss/traversal endpoints, discovers the earliest unavoidable hostile whose aggro envelope intersects the corridor, enrolls only combat-linked social members, and processes multiple pack subgenerations inside one route node before terminaling at the endpoint. This deliberately skips the optional Giant when its aggro envelope does not intersect the southwest route.
- Commit `2c35e2844e` implements the first complete discovery corridor: the four pre-Corborus trash nodes are replaced by one strategic endpoint with no trash GUIDs, entries, centers, radii, or expected counts. Each engaged social pack has its own sequence and truthful ledger terminal, then the same route leg resumes. The node advances only after at least one completed pack, no unresolved/current/forward target, no real combat linkage, and every expected bot is alive in the original instance at the endpoint. Repeated Millhouse retreats are resolved from the live manifest aura/passive state rather than globally masking his GUID.
- Commit `0e21e45218` adds a generic high-density boss-add mode. Exact listed-add density is passed into the deterministic DB profile resolver; `min_enemies`/`max_enemies` remain hard gates and density mode permits only legal AoE/cleave candidates. Ranged damage bots move away from the local add centroid during an alive-but-unattackable configured boss phase. A missing legal area action emits an explicit hold and telemetry event; density mode has no pull or single-target fallback.
- Review found and repaired four pre-run blockers. Commit `2e657d739a` preserves final scripted-transition evidence across discovery subpacks and promotes it to the persistent GUID mask only at the truthful corridor terminal. Commit `32576b99a6` uses the correct relative escape angle, rejects stale shared add focus through the same live-wave predicate, and extends the DB taxonomy with explicit `resource_generator` candidates. Density mode admits only AoE, cleave, or declared resource generators; a legal area action always outranks a generator. Marksmanship Steady Shot and Enhancement Stormstrike are explicitly migrated as generators, so the latter can build Maelstrom for Chain Lightning without a hidden single-target or pull fallback.
- Run 036 on exact revision `49349dba1a` proved the merged corridor live: seven dynamic pack sequences produced 21 real deaths plus three separately scoped Millhouse transitions, zero bot deaths, zero Giant 42810 evidence, and no false route terminal. After the seventh pack, all five bots were alive, moving coherently toward the endpoint at roughly seven yards per tick, with the tank shrinking its remaining distance from 59.26 to 19.38 yards in five seconds. The validator nevertheless stopped on `validation_route_stuck_loop`: it summed the same six historical stuck events from status, summary, and trace into 12, then a single new moving-state healer event arrived before the next progress sample. Historical failures had already been resolved by kills, pack terminals, and movement. The runtime route is sound; the watchdog must deduplicate evidence and count only unresolved same-node/generation failures after the latest strong progress.
- Commits `5765ea86b8` and `c54278b1cb` repair stuck-loop accounting: redundant status/summary/trace views use their maximum rather than a sum, only unresolved failures after same-scope strong progress count, and timestamp-only/sequence-only relations are incomparable and fail closed. Dynamic pack terminals and explicit movement/combat blocker resolutions reset only earlier comparable failures in the same node and generation.
- Run 037 on exact revision `c54278b1cb` truthfully terminaled the discovery corridor after 20 real trash kills and three Millhouse transitions, advanced to Corborus, and kept Giant 42810 at zero references. Corborus GUID 85 fell to 58.67-64.65% before the first wipe; the retry GUID 551 fell from roughly 100% to 66.50-68.30%. The validator still stopped on `semantic_progress_plateau_watchdog` because its health detector accepted only reason `route_target_combat_progress`, while boss snapshots used `route_target_no_health_progress` or `boss_route_no_health_progress`. Density behavior also lasted only a few ticks because submerged Corborus temporarily became unresolvable, despite 17-50 listed adds remaining live. The run had six report deaths, no boss kill, no assistance, and no false terminal.
- Commit `6b889bdf5c` replaces the reason/per-bot health detector with party-shared, ordered boss attempt minima scoped by node, generation, GUID, and entry. Same-scope failure/death/wipe and safe near-full resets establish new attempt baselines; only strict new minima count, while repeated health, healing oscillation, duplicate bot snapshots, and incomparable clocks do not. A real 180-second plateau now receives an explicit failure label.
- Commit `aae1050fe2` latches high-density behavior to the current boss node/generation after observed real boss engagement. A temporarily unresolved boss remains unavailable while at least three listed adds are live; the latch clears on boss return, real kill, swarm end, node reset, or generation change. No encounter IDs, coordinates, or fallback actions were added.
- Review commits `08db528a1b` and `788f6dd5aa` close two false-progress/latch races: ordinary stuck, repath, and target-loss events no longer reset boss attempt minima, while death, repeated death, raid wipe, and instance reset do; a local visibility gap cannot clear the shared density phase because unlatching uses a GUID-deduplicated cohort-wide listed-add scan.
- Run 038 on exact revision `9ada78f33d` proved attempt-aware boss progress and density persistence: the corridor terminaled with 17 real trash kills, zero Giant evidence, and Corborus reached 49.88% on the first attempt and roughly 64.51% on the retry. The validator recorded 124 strict boss-health progress steps and did not plateau falsely. Both attempts then collapsed during listed-add swarms, producing 12 deaths and a truthful death-loop failure. Exact masks show the shared single-add focus was usually outside tank/Enhancement range despite dozens of local adds; ranged centroid movement was reissued until players spread up to 88 yards from the tank, while the excluded healer remained in the swarm and died first in both waves. The density phase is now truthful; action anchoring and movement cohesion are the next runtime blockers.
- Commits `a0c213a908`, `366fa5656a`, and `5a06e7b603` implement and harden the generic density repair. Latched density combat uses deterministic per-bot manifest-listed anchors inside DB profile range; an out-of-band nearest add becomes an explicit range-approach candidate, never a pull or single-target fallback. A tank-authoritative, generation-scoped shared escape point is bounded by the actual living healer's range/LOS and an anchor leash; urgent healing precedes escape, movement is issued once per point, and stalled/misdirected paths are verified and reissued. Escape state clears safely when role anchors disappear, while density lifecycle resets remain tied to swarm end, boss return/kill, or route reset. Independent review approved the combined series.
- Run 039 on exact revision `40905ecdc5` reached the Corborus node with 20 real trash kills and zero deaths, then exposed a pre-combat handler regression: when density was false and no listed add existed, the new `no_compatible_density_anchor` branch still consumed every non-healer tick with `hold_boss_add_density`, so activation remained false and the semantic watchdog correctly stopped the run. Commit `be87b7dbcd` releases the add handler immediately for inactive density/no-add state; explicit approach/hold remains active-density-only.
- Run 040 on exact revision `7eb7980498` proved the cohesive repair: Corborus stayed on real GUID 85 and reached 4.622% health without an encounter reset; 31 distinct Borers died in the first add wave versus five across both run-038 waves; selected area/generator actions rose to 39, deaths fell to nine, and stuck/failure telemetry fell to zero. All five bots recovered after the final death cluster and remained active, but the validator stopped on `validation_route_death_loop` because its label used cumulative `deaths >= 8` while watchdog state correctly showed only one of three repeated danger episodes. The first death cluster had already been followed by 58 strict boss-health minima and 14 kills. This is a cumulative-history validator race, not a runtime terminal. The remaining runtime optimization is keeping tank threat pursuit leashed to the shared escape and reducing noncritical heal casts that interrupt healer relocation.
- Runs 001-022, including aborted runs 017 and 019, are committed as DVC pointers and pushed. Their working copies and the local DVC cache were removed after push; use `pixi run dvc pull <pointer>` to restore one.

Verification:

- `pixi run pytest -q`: 267 passed after scoped stuck accounting, attempt-aware boss-health progress, and density-phase persistence tests.
- `cmake --build build --target worldserver -j2`: passed; exact revision verification is required again after committing the discovery source and progress update.
- DVC remote credentials match the main worktree; `pixi run dvc status` was recorded and reports pre-existing missing-cache drift plus the changed validation stages.
- Every strict live artifact through run 009 was checkpointed and pushed with DVC before local cleanup.

Next handoff:

Unify death-loop label/watchdog semantics around three unresolved, same-scope repeated danger episodes after the latest comparable durable progress; cumulative ordinary deaths must remain outcome data. Then run strict run 041 from exact HEAD before changing the materially improved runtime behavior. Continue requiring zero Giant evidence, real boss kills, and no assistance; checkpoint and push every run before cleanup.
